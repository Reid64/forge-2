/**
 * Build-related types. Re-exported from the canonical definitions in
 * `./index.ts` so consumers can import from a focused module
 * (per the project structure in BLUEPRINT.md).
 */
export type {
  BuildRun,
  BuildStatus,
  PromptExecution,
  PromptExecutionStatus,
} from './index.js';
