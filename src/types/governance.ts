/**
 * Governance document types. Re-exported from the canonical definitions in
 * `./index.ts` (per the project structure in BLUEPRINT.md).
 */
export type {
  GovernanceVersion,
  GovernanceChangeSource,
  StackProfile,
  BrandIdentity,
} from './index.js';
