/**
 * Pattern / learning types. Re-exported from the canonical definitions in
 * `./index.ts` (per the project structure in BLUEPRINT.md).
 */
export type {
  ErrorPattern,
  ErrorCategory,
  Resolution,
  ResolutionType,
  CrossProjectInsight,
  InsightType,
  ProductionTelemetry,
  TelemetryEventType,
  TelemetrySeverity,
  SelfCreatedAgent,
  SelfCreatedAgentStatus,
  DesignPattern,
  DesignPatternType,
} from './index.js';
