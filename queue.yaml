# forge-2 — FORGE 2.0 Build Queue
# Generated by the FORGE Queue Generator (s4-p02) on 2026-07-17T00:23:41.959Z.
# Working directory: C:\Users\manag\Documents\forge-2
# Each prompt is executed via: claude -p --dangerously-skip-permissions
#
# 20 prompt(s), 3 parallel group(s), longest dependency chain 7, ~210100 estimated tokens.
# Standard build order: schema → auth → api → ui → features → agents → dashboards → settings → tests → deploy → verify.
# Build HALTS after governance + queue for Gate 3 (human approval, Contract 2).

- id: schema-migrations
  name: "Database schema & migrations"
  prompt_type: schema
  dependencies: []
  governance_refs: [SCHEMA_REGISTRY.md, BLUEPRINT.md]
  infra: local
  estimated_tokens: 7900
  context_injection:
    schema_sections: [artifact_health_scores, gap_audit_runs]
    behavioral_sections: []
    interaction_maps: []
  description: |
    Create the database schema as migration files under supabase/migrations/. Define all 2 table(s): gap_audit_runs, artifact_health_scores exactly as specified in SCHEMA_REGISTRY.md — every column, type, default, constraint, and foreign key. Create the 13 index(es) and apply the 0 row-level-security policy/policies. Enable RLS on every multi-tenant table (Six Laws Law 1). Load the 2 seed data set(s) defined in SCHEMA_REGISTRY.md. Apply the migrations against the database and confirm every table exists with the correct columns.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: auth-setup
  name: "Authentication & authorization"
  prompt_type: auth
  dependencies: [schema-migrations]
  governance_refs: [BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md]
  infra: local
  estimated_tokens: 7400
  context_injection:
    schema_sections: []
    behavioral_sections: ["Authentication & Authorization"]
    interaction_maps: []
  description: |
    Implement authentication and authorization per BEHAVIORAL_CONTRACTS.md. Define the role(s): operator. Implement middleware.ts as a FULL FILE (never a patch). On ANY role-fetch failure the middleware MUST redirect to /login ONLY — never render a default or wrong-role page (Iron Law 4). Wire the auth flow(s): Operator Login, Authenticated API Request, Human Gate Decision, Resurrect Resume Flow, Session Expiry and Refresh. Multi-tenancy: Single-tenant. No companies table, no tenant_id, no org scoping, no RLS tenant policies. The application is scoped to one operator on one local machine. All data in gap_audit_runs and artifact_health_scores is globally readable by the authenticated operator. The only scoping columns are machine_id (identifying the local machine per SCHEMA_ADDITIONS §0 / Iron Law R6) and user_id (foreign key to auth.users for the single Supabase Auth user). Supabase RLS policies are per-user: a row is accessible if auth.uid() = user_id. There is no multi-user sharing, no invite flow, and no team-level access control. Every protected route derives company_id from the session, never from the request body (Six Laws Law 2).

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: api-audit
  name: "API: audit (5 routes)"
  prompt_type: api
  dependencies: [schema-migrations, auth-setup]
  parallel_group: api-routes
  governance_refs: [BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md]
  infra: local
  estimated_tokens: 10400
  context_injection:
    schema_sections: [artifact_health_scores, gap_audit_runs]
    behavioral_sections: ["API Contracts"]
    interaction_maps: []
  description: |
    Implement the "audit" API route(s) as Next.js App Router route handlers (app/api/.../route.ts):
      - POST /api/audit/run — Initiate a deep governance audit for a given project path. Creates a gap_audit_runs row with status 'running', invokes the GapAuditor pipeline (wrapping ForgeRetrofit runScan + DIAGNOSE builders), scores all nine artifacts, triggers RegenerationEngine for AUTO-tier artifacts, routes CRITICAL/HUMAN_GATE-tier artifacts to HumanGateEvaluator, and updates the row to 'completed', 'failed', or 'halted_for_human'.
      - GET /api/audit/run/:auditRunId — Retrieve the full result of a previously executed audit run, including all artifact health scores, gap counts, halt point reference, and continuation plan.
      - GET /api/audit/runs — List audit runs stored in Build Memory for this machine, with optional filters for status, auditTrigger, and projectPath. Supports pagination.
      - POST /api/audit/run/:auditRunId/gate — Submit a human gate decision (approve or decline) for a specific CRITICAL or HUMAN_GATE-tier artifact gap that was surfaced during an audit halted at 'halted_for_human'. Approve triggers RegenerationEngine for that artifact; decline leaves the artifact as-is and records the declination. If all pending gated items are resolved, the audit status transitions to 'completed'.
      - GET /api/audit/artifact-scores — Query artifact_health_scores rows across audit runs for a given project or build, with optional filters for artifact name and regeneration tier. Used for metric reporting (US-7 success metrics).
    Each handler authenticates the user, derives company_id from the session (NEVER from the request body,
    Six Laws Law 2), validates input, and returns the response shape defined in BEHAVIORAL_CONTRACTS.md.
    Reads/writes only the real tables: artifact_health_scores, gap_audit_runs (no mocks — Iron Law 8).
    Return documented error responses; never leak secrets or stack traces.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: api-resurrect
  name: "API: resurrect (1 route)"
  prompt_type: api
  dependencies: [schema-migrations, auth-setup]
  parallel_group: api-routes
  governance_refs: [BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md]
  infra: local
  estimated_tokens: 5600
  context_injection:
    schema_sections: [artifact_health_scores, gap_audit_runs]
    behavioral_sections: ["API Contracts"]
    interaction_maps: []
  description: |
    Implement the "resurrect" API route(s) as Next.js App Router route handlers (app/api/.../route.ts):
      - POST /api/resurrect/resume — Initiate a build resumption using the continuation plan from a completed audit run. Validates that mean composite_score >= 0.70 and gaps_critical = 0 (R8 / US-8), retrieves the halt_point_reference, and hands the startAtPromptIndex to the Phase 3 executor. Returns the resume command payload; does not re-run completed prompts.
    Each handler authenticates the user, derives company_id from the session (NEVER from the request body,
    Six Laws Law 2), validates input, and returns the response shape defined in BEHAVIORAL_CONTRACTS.md.
    Reads/writes only the real tables: artifact_health_scores, gap_audit_runs (no mocks — Iron Law 8).
    Return documented error responses; never leak secrets or stack traces.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: api-health
  name: "API: health (1 route)"
  prompt_type: api
  dependencies: [schema-migrations, auth-setup]
  parallel_group: api-routes
  governance_refs: [BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md]
  infra: local
  estimated_tokens: 5600
  context_injection:
    schema_sections: [artifact_health_scores, gap_audit_runs]
    behavioral_sections: ["API Contracts"]
    interaction_maps: []
  description: |
    Implement the "health" API route(s) as Next.js App Router route handlers (app/api/.../route.ts):
      - GET /api/health/governance — Return a current governance health summary: latest artifact_health_scores per artifact for a given project, mean composite score, gap counts, and row counts for both Build Memory tables. Satisfies US-7 'forge health' reporting.
    Each handler authenticates the user, derives company_id from the session (NEVER from the request body,
    Six Laws Law 2), validates input, and returns the response shape defined in BEHAVIORAL_CONTRACTS.md.
    Reads/writes only the real tables: artifact_health_scores, gap_audit_runs (no mocks — Iron Law 8).
    Return documented error responses; never leak secrets or stack traces.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: ui-shell
  name: "UI shell, layouts & design tokens"
  prompt_type: ui
  dependencies: [auth-setup]
  governance_refs: [BLUEPRINT.md, BEHAVIORAL_CONTRACTS.md, DESIGN_SYSTEM.md]
  skills: [frontend-design, ui-ux-pro-max]
  infra: local
  estimated_tokens: 17100
  context_injection:
    schema_sections: []
    behavioral_sections: []
    interaction_maps: []
  description: |
    Build the application UI shell: 2 layout(s) (AppShellLayout, GateFocusLayout), the 46 shared component(s), and the design-token system. Apply the design tokens (colors, typography, spacing, radii, shadows) from the architecture as the single source of truth — do not introduce a conflicting palette. Responsive strategy: Mobile-first using Tailwind CSS breakpoints: default (320px+), sm (640px+), md (768px+), lg (1024px+), xl (1280px+). NavigationSidebar collapses to 56px icon-only strip at < 768px and to a bottom sheet drawer at < 640px. ArtifactHealthGrid is 1-column on mobile, 2-column at md, 3-column at lg. All tables become horizontally scrollable with -webkit-overflow-scrolling: touch on mobile. Font sizes scale down one step on mobile (e.g. 2xl headings become xl). Minimum tap target 44x44px on all interactive elements. prefers-reduced-motion media query disables transform and transition animations globally. No horizontal scroll at any breakpoint. Tested breakpoints: 375px (iPhone SE), 768px (iPad), 1024px (iPad Pro landscape), 1440px (desktop). Render real components — no placeholder HTML in public/ (Iron Law 5). Handle empty states.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: feature-dashboardpage
  name: "Feature: DashboardPage"
  prompt_type: feature
  dependencies: [api-health, api-audit, ui-shell]
  parallel_group: features
  governance_refs: [INTERACTION_MAPS.md, BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md, DESIGN_SYSTEM.md]
  skills: [frontend-design, ui-ux-pro-max]
  infra: local
  estimated_tokens: 11400
  context_injection:
    schema_sections: [artifact_health_scores, gap_audit_runs]
    behavioral_sections: ["API Contracts"]
    interaction_maps: ["F20: Deep Codebase Audit: Audit runs list page (/audit/runs) — table rows", "F20: Deep Codebase Audit: Homepage (/) — quick status widget", "F21: Governance Doc Gap Detection: Artifact health scores table (on /audit/runs/:auditRunId)", "F21: Governance Doc Gap Detection: Artifact row expand — gap detail drawer", "F21: Governance Doc Gap Detection: Governance health overview page (/health)", "F24: Continuation from Exact Halt Point: Resurrect page (/resurrect) — build lookup form"]
  description: |
    Build the feature page "DashboardPage" at route /. Purpose: Primary landing view showing overall governance health score, recent audit runs, and quick-action buttons to start a new audit or resume a halted build. Surfaces the mean composite_score, gap counts, and any open human gates at a glance.. Wire it to the API endpoint(s): GET /api/health/governance, GET /api/audit/runs, GET /api/audit/artifact-scores. Compose the component(s): GovernanceHealthBanner, RecentAuditRunsTable, QuickActionPanel, OpenGatesAlert, ArtifactHealthGrid, EmptyAuditState. Implement every interactive element per INTERACTION_MAPS.md (Artifact health scores table (on /audit/runs/:auditRunId), Artifact row expand — gap detail drawer, Audit runs list page (/audit/runs) — table rows, Governance health overview page (/health), Homepage (/) — quick status widget, Resurrect page (/resurrect) — build lookup form) — each with its full action → API call → DB write → success/error response chain (Contract 18). Use only real API calls to real tables — no mock data (Iron Law 8). Handle loading and empty states.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: feature-newauditpage
  name: "Feature: NewAuditPage"
  prompt_type: feature
  dependencies: [api-audit, ui-shell]
  parallel_group: features
  governance_refs: [INTERACTION_MAPS.md, BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md, DESIGN_SYSTEM.md]
  skills: [frontend-design, ui-ux-pro-max]
  infra: local
  estimated_tokens: 15600
  context_injection:
    schema_sections: [artifact_health_scores, gap_audit_runs]
    behavioral_sections: ["API Contracts"]
    interaction_maps: ["F20: Deep Codebase Audit: Audit New button (navbar)", "F20: Deep Codebase Audit: Audit run detail page (/audit/runs/:auditRunId) — summary card", "F20: Deep Codebase Audit: Audit run filter bar (/audit/runs)", "F20: Deep Codebase Audit: Audit run list row — click to detail", "F20: Deep Codebase Audit: Audit run status polling (auto-refresh on /audit/runs/:auditRunId)", "F20: Deep Codebase Audit: Audit runs list page (/audit/runs) — table rows", "F20: Deep Codebase Audit: Audit scope selector (/audit/new)", "F20: Deep Codebase Audit: Audit trigger type selector (/audit/new)", "F20: Deep Codebase Audit: Build Run ID input (visible when trigger=halt_recovery)", "F20: Deep Codebase Audit: Homepage (/) — quick status widget", "F20: Deep Codebase Audit: Non-interactive mode toggle (/audit/new)", "F20: Deep Codebase Audit: Project path input field (/audit/new)", "F20: Deep Codebase Audit: Run Audit submit button (/audit/new)", "F20: Deep Codebase Audit: Sidebar navigation — active state"]
  description: |
    Build the feature page "NewAuditPage" at route /audit/new. Purpose: Form to configure and launch a new audit run. Operator specifies project path, audit scope (FULL, CODE_ONLY, GOVERNANCE_ONLY, TARGETED), and whether it is a halt-recovery audit. Submits to POST /api/audit/run and redirects to the audit detail page.. Wire it to the API endpoint(s): POST /api/audit/run. Compose the component(s): AuditConfigForm, ScopeSelector, HaltRecoveryToggle, ProjectPathInput, AuditSubmitButton, FormValidationSummary. Implement every interactive element per INTERACTION_MAPS.md (Audit New button (navbar), Audit run detail page (/audit/runs/:auditRunId) — summary card, Audit run filter bar (/audit/runs), Audit run list row — click to detail, Audit run status polling (auto-refresh on /audit/runs/:auditRunId), Audit runs list page (/audit/runs) — table rows, Audit scope selector (/audit/new), Audit trigger type selector (/audit/new), Build Run ID input (visible when trigger=halt_recovery), Homepage (/) — quick status widget, Non-interactive mode toggle (/audit/new), Project path input field (/audit/new), Run Audit submit button (/audit/new), Sidebar navigation — active state) — each with its full action → API call → DB write → success/error response chain (Contract 18). Use only real API calls to real tables — no mock data (Iron Law 8). Handle loading and empty states.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: feature-auditrunslistpage
  name: "Feature: AuditRunsListPage"
  prompt_type: feature
  dependencies: [api-audit, ui-shell]
  parallel_group: features
  governance_refs: [INTERACTION_MAPS.md, BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md, DESIGN_SYSTEM.md]
  skills: [frontend-design, ui-ux-pro-max]
  infra: local
  estimated_tokens: 16100
  context_injection:
    schema_sections: [artifact_health_scores, gap_audit_runs]
    behavioral_sections: ["API Contracts"]
    interaction_maps: ["F20: Deep Codebase Audit: Audit New button (navbar)", "F20: Deep Codebase Audit: Audit run detail page (/audit/runs/:auditRunId) — summary card", "F20: Deep Codebase Audit: Audit run filter bar (/audit/runs)", "F20: Deep Codebase Audit: Audit run list row — click to detail", "F20: Deep Codebase Audit: Audit run status polling (auto-refresh on /audit/runs/:auditRunId)", "F20: Deep Codebase Audit: Audit runs list page (/audit/runs) — table rows", "F20: Deep Codebase Audit: Audit scope selector (/audit/new)", "F20: Deep Codebase Audit: Audit trigger type selector (/audit/new)", "F20: Deep Codebase Audit: Build Run ID input (visible when trigger=halt_recovery)", "F20: Deep Codebase Audit: Homepage (/) — quick status widget", "F20: Deep Codebase Audit: Non-interactive mode toggle (/audit/new)", "F20: Deep Codebase Audit: Project path input field (/audit/new)", "F20: Deep Codebase Audit: Run Audit submit button (/audit/new)", "F20: Deep Codebase Audit: Sidebar navigation — active state", "F24: Continuation from Exact Halt Point: Resurrect page (/resurrect) — build lookup form"]
  description: |
    Build the feature page "AuditRunsListPage" at route /audit/runs. Purpose: Paginated list of all gap_audit_runs records ordered by created_at descending. Each row shows audit_id, trigger, status badge, health_score_before/after, gap counts, and duration. Supports filtering by status and audit_trigger.. Wire it to the API endpoint(s): GET /api/audit/runs. Compose the component(s): AuditRunsTable, AuditStatusBadge, AuditRunFilterBar, PaginationControls, EmptyAuditRunsState. Implement every interactive element per INTERACTION_MAPS.md (Audit New button (navbar), Audit run detail page (/audit/runs/:auditRunId) — summary card, Audit run filter bar (/audit/runs), Audit run list row — click to detail, Audit run status polling (auto-refresh on /audit/runs/:auditRunId), Audit runs list page (/audit/runs) — table rows, Audit scope selector (/audit/new), Audit trigger type selector (/audit/new), Build Run ID input (visible when trigger=halt_recovery), Homepage (/) — quick status widget, Non-interactive mode toggle (/audit/new), Project path input field (/audit/new), Resurrect page (/resurrect) — build lookup form, Run Audit submit button (/audit/new), Sidebar navigation — active state) — each with its full action → API call → DB write → success/error response chain (Contract 18). Use only real API calls to real tables — no mock data (Iron Law 8). Handle loading and empty states.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: feature-auditrundetailpage
  name: "Feature: AuditRunDetailPage"
  prompt_type: feature
  dependencies: [api-audit, ui-shell]
  parallel_group: features
  governance_refs: [INTERACTION_MAPS.md, BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md, DESIGN_SYSTEM.md]
  skills: [frontend-design, ui-ux-pro-max]
  infra: local
  estimated_tokens: 19800
  context_injection:
    schema_sections: [artifact_health_scores, gap_audit_runs]
    behavioral_sections: ["API Contracts"]
    interaction_maps: ["F20: Deep Codebase Audit: Audit New button (navbar)", "F20: Deep Codebase Audit: Audit run detail page (/audit/runs/:auditRunId) — summary card", "F20: Deep Codebase Audit: Audit run filter bar (/audit/runs)", "F20: Deep Codebase Audit: Audit run list row — click to detail", "F20: Deep Codebase Audit: Audit run status polling (auto-refresh on /audit/runs/:auditRunId)", "F20: Deep Codebase Audit: Audit runs list page (/audit/runs) — table rows", "F20: Deep Codebase Audit: Audit scope selector (/audit/new)", "F20: Deep Codebase Audit: Audit trigger type selector (/audit/new)", "F20: Deep Codebase Audit: Build Run ID input (visible when trigger=halt_recovery)", "F20: Deep Codebase Audit: Homepage (/) — quick status widget", "F20: Deep Codebase Audit: Non-interactive mode toggle (/audit/new)", "F20: Deep Codebase Audit: Project path input field (/audit/new)", "F20: Deep Codebase Audit: Run Audit submit button (/audit/new)", "F20: Deep Codebase Audit: Sidebar navigation — active state", "F21: Governance Doc Gap Detection: Artifact health scores table (on /audit/runs/:auditRunId)", "F21: Governance Doc Gap Detection: Artifact row expand — gap detail drawer", "F23: Human Gate on Major Architectural Gaps: Gate approval confirmation modal — Confirm button", "F23: Human Gate on Major Architectural Gaps: Gate decline inline confirmation — Yes, decline link", "F23: Human Gate on Major Architectural Gaps: Human gate review page (/gate/:auditRunId)"]
  description: |
    Build the feature page "AuditRunDetailPage" at route /audit/runs/:auditRunId. Purpose: Full detail view for a single gap_audit_runs record. Displays artifact health scores table, gap severity breakdown, halt_point_reference (if present), continuation_plan, regeneration log, and any open human gate items needing operator action. Entry point for gate approve/decline.. Wire it to the API endpoint(s): GET /api/audit/run/:auditRunId, GET /api/audit/artifact-scores, POST /api/audit/run/:auditRunId/gate. Compose the component(s): AuditRunHeader, AuditStatusBadge, ArtifactHealthScoresTable, GapSeverityBreakdown, HaltPointCard, ContinuationPlanPanel, RegenerationLogAccordion, HumanGateReviewPanel, ScoreGauge, EmptyGapsState. Implement every interactive element per INTERACTION_MAPS.md (Artifact health scores table (on /audit/runs/:auditRunId), Artifact row expand — gap detail drawer, Audit New button (navbar), Audit run detail page (/audit/runs/:auditRunId) — summary card, Audit run filter bar (/audit/runs), Audit run list row — click to detail, Audit run status polling (auto-refresh on /audit/runs/:auditRunId), Audit runs list page (/audit/runs) — table rows, Audit scope selector (/audit/new), Audit trigger type selector (/audit/new), Build Run ID input (visible when trigger=halt_recovery), Gate approval confirmation modal — Confirm button, Gate decline inline confirmation — Yes, decline link, Homepage (/) — quick status widget, Human gate review page (/gate/:auditRunId), Non-interactive mode toggle (/audit/new), Project path input field (/audit/new), Run Audit submit button (/audit/new), Sidebar navigation — active state) — each with its full action → API call → DB write → success/error response chain (Contract 18). Use only real API calls to real tables — no mock data (Iron Law 8). Handle loading and empty states.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: feature-resurrectpage
  name: "Feature: ResurrectPage"
  prompt_type: feature
  dependencies: [api-health, api-audit, api-resurrect, ui-shell]
  parallel_group: features
  governance_refs: [INTERACTION_MAPS.md, BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md, DESIGN_SYSTEM.md]
  skills: [frontend-design, ui-ux-pro-max]
  infra: local
  estimated_tokens: 11100
  context_injection:
    schema_sections: [artifact_health_scores, gap_audit_runs]
    behavioral_sections: ["API Contracts"]
    interaction_maps: ["F20: Deep Codebase Audit: Audit runs list page (/audit/runs) — table rows", "F20: Deep Codebase Audit: Homepage (/) — quick status widget", "F21: Governance Doc Gap Detection: Governance health overview page (/health)", "F24: Continuation from Exact Halt Point: Resurrect page (/resurrect) — build lookup form", "F24: Continuation from Halt Point: Resume Build button (/resurrect)"]
  description: |
    Build the feature page "ResurrectPage" at route /resurrect. Purpose: Operator interface to initiate a build resumption. Shows the resolved halt point facts (buildRunId, promptIndex, promptName, failingCheck, subStepIndex, feature branch), the health floor check (mean composite_score >= 0.70, gaps_critical = 0), and a prominent Resume button. Displays a refusal message with per-artifact details when the floor is not met.. Wire it to the API endpoint(s): GET /api/health/governance, GET /api/audit/runs, POST /api/resurrect/resume. Compose the component(s): HaltPointSummaryCard, HealthFloorCheckPanel, ResumeBuildButton, ResumptionRefusalAlert, BranchNameDisplay, PromptIndexIndicator, FailingCheckDetail, EmptyResurrectState. Implement every interactive element per INTERACTION_MAPS.md (Audit runs list page (/audit/runs) — table rows, Governance health overview page (/health), Homepage (/) — quick status widget, Resume Build button (/resurrect), Resurrect page (/resurrect) — build lookup form) — each with its full action → API call → DB write → success/error response chain (Contract 18). Use only real API calls to real tables — no mock data (Iron Law 8). Handle loading and empty states.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: feature-governancehealthpage
  name: "Feature: GovernanceHealthPage"
  prompt_type: feature
  dependencies: [api-health, api-audit, ui-shell]
  parallel_group: features
  governance_refs: [INTERACTION_MAPS.md, BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md, DESIGN_SYSTEM.md]
  skills: [frontend-design, ui-ux-pro-max]
  infra: local
  estimated_tokens: 10200
  context_injection:
    schema_sections: [artifact_health_scores, gap_audit_runs]
    behavioral_sections: ["API Contracts"]
    interaction_maps: ["F20: Deep Codebase Audit: Homepage (/) — quick status widget", "F21: Governance Doc Gap Detection: Artifact health scores table (on /audit/runs/:auditRunId)", "F21: Governance Doc Gap Detection: Artifact row expand — gap detail drawer", "F21: Governance Doc Gap Detection: Governance health overview page (/health)"]
  description: |
    Build the feature page "GovernanceHealthPage" at route /health. Purpose: Detailed governance health dashboard. Shows per-artifact completeness_score, freshness_score, consistency_score, and composite_score as visual gauges and a sortable table. Highlights artifacts below the 0.70 floor. Links to the most recent audit run for each artifact.. Wire it to the API endpoint(s): GET /api/health/governance, GET /api/audit/artifact-scores. Compose the component(s): ArtifactHealthGrid, ArtifactScoreGauge, ArtifactScoresDetailTable, HealthFloorIndicator, ArtifactDriftDetail, MissingSecionslist, PlaceholderCountBadge, EmptyHealthState. Implement every interactive element per INTERACTION_MAPS.md (Artifact health scores table (on /audit/runs/:auditRunId), Artifact row expand — gap detail drawer, Governance health overview page (/health), Homepage (/) — quick status widget) — each with its full action → API call → DB write → success/error response chain (Contract 18). Use only real API calls to real tables — no mock data (Iron Law 8). Handle loading and empty states.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: feature-humangatepage
  name: "Feature: HumanGatePage"
  prompt_type: feature
  dependencies: [api-audit, ui-shell]
  parallel_group: features
  governance_refs: [INTERACTION_MAPS.md, BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md, DESIGN_SYSTEM.md]
  skills: [frontend-design, ui-ux-pro-max]
  infra: local
  estimated_tokens: 14900
  context_injection:
    schema_sections: [artifact_health_scores, gap_audit_runs]
    behavioral_sections: ["API Contracts"]
    interaction_maps: ["F20: Deep Codebase Audit: Audit run detail page (/audit/runs/:auditRunId) — summary card", "F20: Deep Codebase Audit: Audit run status polling (auto-refresh on /audit/runs/:auditRunId)", "F23: Human Gate on Major Architectural Gaps: Approve Regeneration button on gate gap card", "F23: Human Gate on Major Architectural Gaps: Decline / Keep As-Is button on gate gap card", "F23: Human Gate on Major Architectural Gaps: Gap detail card on gate page — plain language description", "F23: Human Gate on Major Architectural Gaps: Gate approval confirmation modal — Cancel button", "F23: Human Gate on Major Architectural Gaps: Gate approval confirmation modal — Confirm button", "F23: Human Gate on Major Architectural Gaps: Gate decline inline confirmation — Yes, decline link", "F23: Human Gate on Major Architectural Gaps: Gate page — all gates resolved state", "F23: Human Gate on Major Architectural Gaps: halted_for_human status banner on audit detail page", "F23: Human Gate on Major Architectural Gaps: Human gate review page (/gate/:auditRunId)", "F23: Human Gate on Major Architectural Gaps: Non-interactive mode gate deferral notice"]
  description: |
    Build the feature page "HumanGatePage" at route /gate/:auditRunId. Purpose: Dedicated, focused page for a single human gate review session. Presents one gated artifact at a time with plain-language description of the gap, what regeneration would change, and Approve / Decline actions. Non-dismissable until the operator makes a decision or explicitly defers. Matches F23 structural gate requirements.. Wire it to the API endpoint(s): GET /api/audit/run/:auditRunId, POST /api/audit/run/:auditRunId/gate. Compose the component(s): GateDecisionCard, GapPlainLanguageSummary, RegenerationImpactPreview, ApproveDeclineActions, GateProgressIndicator, ArtifactSeverityLabel, DeferGateButton. Implement every interactive element per INTERACTION_MAPS.md (Approve Regeneration button on gate gap card, Audit run detail page (/audit/runs/:auditRunId) — summary card, Audit run status polling (auto-refresh on /audit/runs/:auditRunId), Decline / Keep As-Is button on gate gap card, Gap detail card on gate page — plain language description, Gate approval confirmation modal — Cancel button, Gate approval confirmation modal — Confirm button, Gate decline inline confirmation — Yes, decline link, Gate page — all gates resolved state, halted_for_human status banner on audit detail page, Human gate review page (/gate/:auditRunId), Non-interactive mode gate deferral notice) — each with its full action → API call → DB write → success/error response chain (Contract 18). Use only real API calls to real tables — no mock data (Iron Law 8). Handle loading and empty states.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: feature-f21-governance-doc-gap-detection
  name: "Feature: F21: Governance Doc Gap Detection"
  prompt_type: feature
  dependencies: [ui-shell]
  parallel_group: features
  governance_refs: [INTERACTION_MAPS.md, BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md, DESIGN_SYSTEM.md]
  skills: [frontend-design, ui-ux-pro-max]
  infra: local
  estimated_tokens: 7800
  context_injection:
    schema_sections: []
    behavioral_sections: ["API Contracts"]
    interaction_maps: ["F21: Governance Doc Gap Detection: Drift detected badge on artifact row", "F21: Governance Doc Gap Detection: Missing sections pill list in artifact gap drawer", "F21: Governance Doc Gap Detection: Placeholder count display in artifact gap drawer"]
  description: |
    Build the "F21: Governance Doc Gap Detection" feature, implementing every interactive element per INTERACTION_MAPS.md (Drift detected badge on artifact row, Missing sections pill list in artifact gap drawer, Placeholder count display in artifact gap drawer). Each element follows the full action → API call → DB write → success/error response chain (Contract 18). Use real API calls to real tables — no mock data (Iron Law 8).

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: feature-f22-autonomous-artifact-regeneration
  name: "Feature: F22: Autonomous Artifact Regeneration"
  prompt_type: feature
  dependencies: [ui-shell]
  parallel_group: features
  governance_refs: [INTERACTION_MAPS.md, BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md, DESIGN_SYSTEM.md]
  skills: [frontend-design, ui-ux-pro-max]
  infra: local
  estimated_tokens: 8400
  context_injection:
    schema_sections: []
    behavioral_sections: ["API Contracts"]
    interaction_maps: ["F22: Autonomous Artifact Regeneration: Auto-regeneration count card (on audit detail summary)", "F22: Autonomous Artifact Regeneration: Phase 3 regeneration refusal banner", "F22: Autonomous Artifact Regeneration: Regeneration status indicator on artifact row", "F22: Autonomous Artifact Regeneration: Regeneration tier badge on artifact row"]
  description: |
    Build the "F22: Autonomous Artifact Regeneration" feature, implementing every interactive element per INTERACTION_MAPS.md (Auto-regeneration count card (on audit detail summary), Phase 3 regeneration refusal banner, Regeneration status indicator on artifact row, Regeneration tier badge on artifact row). Each element follows the full action → API call → DB write → success/error response chain (Contract 18). Use real API calls to real tables — no mock data (Iron Law 8).

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: feature-f24-continuation-from-halt-point
  name: "Feature: F24: Continuation from Halt Point"
  prompt_type: feature
  dependencies: [ui-shell]
  parallel_group: features
  governance_refs: [INTERACTION_MAPS.md, BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md, DESIGN_SYSTEM.md]
  skills: [frontend-design, ui-ux-pro-max]
  infra: local
  estimated_tokens: 10200
  context_injection:
    schema_sections: []
    behavioral_sections: ["API Contracts"]
    interaction_maps: ["F24: Continuation from Halt Point: Continuation plan accordion (/resurrect — post-resume)", "F24: Continuation from Halt Point: Copy CLI command button on continuation plan", "F24: Continuation from Halt Point: Dialtest attempt 5 fixture verification badge", "F24: Continuation from Halt Point: Halt point preview card (/resurrect)", "F24: Continuation from Halt Point: Health floor refusal card (/resurrect)", "F24: Continuation from Halt Point: No resumable state message (/resurrect)", "F24: Continuation from Halt Point: Recent halted builds dropdown (/resurrect)"]
  description: |
    Build the "F24: Continuation from Halt Point" feature, implementing every interactive element per INTERACTION_MAPS.md (Continuation plan accordion (/resurrect — post-resume), Copy CLI command button on continuation plan, Dialtest attempt 5 fixture verification badge, Halt point preview card (/resurrect), Health floor refusal card (/resurrect), No resumable state message (/resurrect), Recent halted builds dropdown (/resurrect)). Each element follows the full action → API call → DB write → success/error response chain (Contract 18). Use real API calls to real tables — no mock data (Iron Law 8).

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: tests-e2e
  name: "End-to-end (Playwright) tests"
  prompt_type: test
  dependencies: [feature-dashboardpage, feature-newauditpage, feature-auditrunslistpage, feature-auditrundetailpage, feature-resurrectpage, feature-governancehealthpage, feature-humangatepage, feature-f21-governance-doc-gap-detection, feature-f22-autonomous-artifact-regeneration, feature-f24-continuation-from-halt-point]
  parallel_group: tests
  governance_refs: [TESTING.md, BEHAVIORAL_CONTRACTS.md]
  infra: local
  estimated_tokens: 10800
  context_injection:
    schema_sections: []
    behavioral_sections: ["Interaction Contracts (summary)"]
    interaction_maps: ["F20: Deep Codebase Audit", "F21: Governance Doc Gap Detection", "F22: Autonomous Artifact Regeneration", "F23: Human Gate on Major Architectural Gaps", "F24: Continuation from Exact Halt Point", "F24: Continuation from Halt Point"]
  description: |
    Write the Playwright end-to-end test suite per TESTING.md (specs: Audit New - Full Flow, Audit New - Halt Recovery Mode, Audit Runs List, Audit Detail - Artifact Scores, Human Gate - Non-Interactive Deferral, Resurrect Resume - Happy Path, Resurrect Resume - Floor Enforcement, Health Dashboard, Home Page Navigation, AUTO Regeneration - Score Improvement). Cover the key user flows across every page, exercising navigation, forms, and role-based access. Tests must verify real API calls to real tables (no mocks) and pass 10/10 before the build advances.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: tests-api
  name: "API tests"
  prompt_type: test
  dependencies: [api-audit, api-resurrect, api-health]
  parallel_group: tests
  governance_refs: [TESTING.md, BEHAVIORAL_CONTRACTS.md]
  infra: local
  estimated_tokens: 8200
  context_injection:
    schema_sections: []
    behavioral_sections: ["API Contracts"]
    interaction_maps: []
  description: |
    Write the API test suite per TESTING.md for the 7 route(s): POST /api/audit/run; GET /api/audit/run/:auditRunId; GET /api/audit/runs; POST /api/audit/run/:auditRunId/gate; POST /api/resurrect/resume; GET /api/health/governance; GET /api/audit/artifact-scores. Verify each route's auth requirement, response status and shape, company scoping, and documented errors.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: deploy
  name: "Deploy to production"
  prompt_type: deploy
  dependencies: [tests-e2e, tests-api]
  governance_refs: [BLUEPRINT.md]
  infra: local
  estimated_tokens: 5500
  context_injection:
    schema_sections: []
    behavioral_sections: []
    interaction_maps: []
  description: |
    Deploy the application following the FORGE deployment protocol: tsc --noEmit → build → deploy → tests pass 10/10. Ensure every required environment variable is set: FORGE_AUDIT_MAX_DURATION_MS, FORGE_AUDIT_REGEN_THRESHOLD, FORGE_HEALTH_FLOOR_SCORE, FORGE_LOG_LEVEL, FORGE_MACHINE_ID, FORGE_MEMORY_DB_PATH, FORGE_NON_INTERACTIVE, FORGE_PROJECT_ROOT, FORGE_SCAN_TIMEOUT_MS, FORGE_WEBHOOK_SECRET, NEXT_PUBLIC_SUPABASE_ANON_KEY, NEXT_PUBLIC_SUPABASE_URL, NODE_ENV, OPENAI_API_KEY, OPENAI_MAX_TOKENS, OPENAI_MODEL, SENTRY_AUTH_TOKEN, SENTRY_DSN, SENTRY_ORG, SENTRY_PROJECT, SUPABASE_DB_URL, SUPABASE_SERVICE_ROLE_KEY, VERCEL_ENV, VERCEL_REGION, VERCEL_URL. Deploy config: Platform: Vercel (Next.js 14 App Router). Package manager: pnpm. Build command: `pnpm build`. Output: `.next`. Install command: `pnpm install --frozen-lockfile`. Node runtime: 20.x. Serverless function region: iad1 (us-east-1) as default; configurable via VERCEL_REGION. Function max duration: 300s for `/api/audit/run` (POST) and `/api/resurrect/resume` (POST) to accommodate ForgeRetrofit full scan; all other API routes use default 10s. Edge runtime: NOT used — audit routes require Node.js APIs (fs, child_process, SQLite). Static pages (`/`, `/health`, `/gate/:auditRunId`) use ISR with revalidate=60. Supabase project hosted in same AWS region as Vercel function. Database migrations run via `supabase db push` in CI before Vercel deploy. Branch deployments enabled for preview; FORGE_NON_INTERACTIVE=true forced on all preview deployments. Production domain configured via Vercel custom domain. Cron: none (audits are operator-triggered, not scheduled). pnpm workspace root; single app package at repo root. TypeScript strict mode enforced; `tsc --noEmit` runs in CI before build.. Abort the deploy if any gate fails — never force-push broken code.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: verify-six-laws
  name: "Six Laws verification"
  prompt_type: test
  dependencies: [deploy]
  governance_refs: [TESTING.md, BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md]
  infra: local
  estimated_tokens: 6100
  context_injection:
    schema_sections: [artifact_health_scores, gap_audit_runs]
    behavioral_sections: ["API Contracts", "Authentication & Authorization"]
    interaction_maps: []
  description: |
    Run automated Six Laws verification (Contract 19) for the 2 table(s), 7 route(s), and 7 page(s): Law 1 SCHEMA — every table from SCHEMA_REGISTRY.md exists with correct columns and RLS; Law 2 API — every endpoint responds with the contracted status and shape; Law 3 UI — every page renders with no placeholder/"coming soon" text; Law 4 DATA — pages make real API calls to real tables (no mock data); Law 5 WIRING — navigation, forms, and role gates work end-to-end. Laws 1-5 must ALL pass; Law 6 (human verification in the browser) is the final Gate.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.
