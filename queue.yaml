project: forge-2
governance:
- BLUEPRINT.md
- STATE_OF_THE_BUILD.md
- SESSION_STATE.md
- SCHEMA_REGISTRY.md
- BEHAVIORAL_CONTRACTS.md
prompts:
  - id: stage3-testing-wiring
    name: Wire Trivy, Gitleaks, Lighthouse, axe-core, visual-regression, SEO, migration-safety into TestOrchestrator
    prompt: "Per upgrades\\SYSTEMS-5-9-GAP-MATRIX.md Section 3: wire the following already-working phase4-sentinel.ts validation checks into TestOrchestrator so their results get written to test_run_results via the existing persist.ts pattern used by the current 7 runners: Trivy dependency/container scan (phase4-sentinel.ts Ring3a, approx lines 2109-2202) to TestSuiteDb.DEPENDENCY_SCAN or closest match; Gitleaks secret scan (Ring3b, approx lines 2219-2295) to TestSuiteDb.SECRET_SCAN or closest match; Lighthouse (Ring3c, approx lines 2352-2498) to TestSuiteDb.LIGHTHOUSE or closest match; axe-core accessibility (src/tools/accessibility-auditor.ts) to TestSuiteDb.ACCESSIBILITY or closest match; Visual regression (src/tools/visual-regression.ts) to TestSuiteDb.VISUAL_REGRESSION; SEO/broken-link (src/tools/seo-validator.ts) to TestSuiteDb.SEO or closest match; Migration safety (src/tools/migration-safety.ts) to TestSuiteDb.MIGRATION or closest match. For each: create a thin adapter in src/testing/runners/ following the exact pattern of the existing 7 runners (input RunnerInput, output RunnerOutcome, call persistRunnerOutcome from persist.ts). Do not duplicate scanning logic - each adapter calls the existing phase4-sentinel.ts function or src/tools/*.ts module directly. Do not modify phase4-sentinel.ts's existing Sentinel gate behavior. Register all 7 new runners in orchestrator.ts's runner list. First list the exact TestSuiteDb enum values from src/memory/test-results.ts before writing any code, use only those exact values. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(testing): wire Trivy, Gitleaks, Lighthouse, axe-core, visual-regression, SEO, migration-safety into TestOrchestrator'."
    gates:
    - type: compile

  - id: stage4-traceability-invariants
    name: Build requirements traceability and invariant engine
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\upgrades\\ENGINEERING_COMPLETENESS.md ontology/traceability and invariant-engine sections. Create src\\governance\\traceability.ts exporting a lightweight REQ-ID scheme: parseRequirementIds(text): string[] extracting REQ-NNN patterns from PRD.md and BLUEPRINT.md, and traceRequirement(reqId, projectPath): TraceResult searching queue YAML prompt text, git commit messages, and test_run_results for references to that ID, reporting which stage (implemented/tested/deployed) it has reached. Create src\\governance\\invariants.ts exporting a small starter set of machine-checked invariants grounded in existing behavioral contracts (BEHAVIORAL_CONTRACTS.md) rather than invented ones - e.g. Contract 3 (no regen during active Phase 3 build) becomes checkInvariant('no-write-during-build'). Export checkAllInvariants(projectPath): InvariantResult[] called at Phase 3 pre-write and Phase 5 end. Register forge trace <req-id> command. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(governance): requirements traceability and invariant engine grounded in existing contracts'."
    gates:
    - type: compile

  - id: stage4-state-machine-blast-radius
    name: Build formal build state machine and change-impact blast-radius analysis
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\upgrades\\ENGINEERING_COMPLETENESS.md state-machine and blast-radius sections. Create src\\governance\\build-state-machine.ts exporting BuildState enum (CONCEPT, PLANNING, IMPLEMENTATION, VALIDATION, HARDENING, RELEASE_CANDIDATE, APPROVAL, DEPLOYMENT, OBSERVATION matching existing phase1-5 concepts) and PromptState enum (PLANNED, READY, RUNNING, BLOCKED, FAILED, RETRYING, VALIDATING, PASSED) mapped onto the existing prompt_executions status column - do not add a new table, add a computed mapping function mapExecutionToState(row). Create src\\governance\\blast-radius.ts exporting analyzeBlastRadius(changedFilePath, projectPath): ImpactResult using existing dependency-graph data already computed by parallel-scheduler.ts to find downstream files/tests likely affected by a change. Wire analyzeBlastRadius into phase3-executor.ts to log affected-files list before each prompt's Sentinel run. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(governance): formal build state machine and blast-radius analysis'."
    gates:
    - type: compile

  - id: stage4-adr-risk-debt
    name: Build ADR provenance log, assumption registry, risk register, tech-debt ledger
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\upgrades\\ENGINEERING_COMPLETENESS.md decision-provenance, assumption-registry, risk-register, and tech-debt sections. Create governance\\adr\\ directory convention and src\\governance\\adr.ts exporting recordDecision(decision, evidence, alternatives, confidence): writes a numbered ADR-NNN.md file using the existing confidence-scorer.ts output where available. Create src\\governance\\assumptions.ts, src\\governance\\risk-register.ts, src\\governance\\tech-debt.ts each exporting a typed CRUD module backed by new SQLite tables (assumptions, risks, tech_debt) added to src\\learning\\database.ts initializeForgeMemory, added to ALL_FORGE_TABLES, schema version bumped. Register forge adr, forge risk, forge debt CLI subcommands listing current entries. Wire tech-debt severity into the readiness-level DoD check from stage2-readiness-dod as a blocking condition at ENTERPRISE-GRADE and above. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(governance): ADR provenance, assumption registry, risk register, tech-debt ledger'."
    gates:
    - type: compile

  - id: stage4-deadloop
    name: Build dead-loop and stagnation detection
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\upgrades\\ENGINEERING_COMPLETENESS.md dead-loop and stagnation sections. Create src\\integration\\loop-detector.ts exporting detectDeadLoop(buildRunId, db): boolean querying prompt_executions for the same error_signature appearing 3+ times in the current build run's Sentinel failures. When true, halt automatic retry and instead trigger src\\tools\\consensus-validator.ts for a diagnostic pass before allowing further attempts, and log STAGNATION DETECTED to STATE_OF_THE_BUILD.md. Wire into phase3-executor.ts's existing retry loop right before the current max-retries check. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(integration): dead-loop and stagnation detection halts blind retries'."
    gates:
    - type: compile

  - id: stage5-observability
    name: Build Control Plane run telemetry (.forge/runs/*.jsonl)
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\upgrades\\CAPABILITIES_MEMO.md observability section. Create src\\telemetry\\run-recorder.ts exporting a RunRecorder class writing structured JSONL to .forge\\runs\\<run-id>\\ : events.jsonl (every phase transition), prompts.jsonl (per-prompt start/end/gate results, already available from prompt_executions - stream not duplicate), tests.jsonl (from test_run_results as they're written), failures.jsonl, metrics.json (summary counts). Wire RunRecorder into phase3-executor.ts at the same points renderProgress already logs to console - this mirrors console output to structured files, does not replace console output. On Phase 5 end write final-report.md summarizing the run in the same style as the existing console completion banner. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(telemetry): structured .forge/runs JSONL observability alongside console output'."
    gates:
    - type: compile

  - id: stage6-consensus-upgrade
    name: Upgrade Consensus Engine to independent proposals plus critique round plus Perplexity
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\src\\tools\\consensus-validator.ts fully, especially the provider list around line 157-162 and the synthesis logic around 727-763. Currently it validates one already-generated artifact across providers. Add a new function generateIndependentProposals(topic, context, providers): ProposalSet that asks each configured provider (anthropic, openai, gemini, deepseek) to independently propose a solution to the same topic before any synthesis happens, then add a critiqueRound(proposals): CritiqueSet where each provider reviews the other providers' proposals for contradictions - reuse the existing contradiction-clustering logic already in the file rather than rewriting it. Add Perplexity as a 5th provider specifically for the research/evidence-gathering role, wired only into generateIndependentProposals's evidence-collection step, not full consensus voting. Wire generateIndependentProposals into phase1b-architect.ts as an optional pre-step for NEW BUILD mode only, flagged by manifest.yaml consensus_engine setting. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(consensus): independent per-provider proposals, critique round, Perplexity evidence role'."
    gates:
    - type: compile

  - id: stage7-queue-concurrency
    name: Enable deferred concurrent execution in parallel-scheduler
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\src\\engine\\parallel-scheduler.ts fully and the comment marking concurrent execution as deferred, and phase3-executor.ts lines around 920-921 where parallel_group is passed but not acted on. Implement actual concurrent spawning: when consecutive queue prompts share the same parallel_group and have no file-path overlap (check via blast-radius.ts from stage4-state-machine-blast-radius), run them as separate Claude Code CLI subprocess calls concurrently via Promise.all, each with its own Sentinel gate, and only advance the queue once all prompts in the group have resolved. If any prompt in a concurrent group fails its gate, halt the whole group and do not auto-commit partial group results. Respect a configurable max-concurrency from manifest.yaml, default 2. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(scheduler): enable concurrent execution for non-overlapping parallel_group prompts'."
    gates:
    - type: compile

  - id: stage8-design-intelligence
    name: Build App Profiler, Design Router, Design Tournament, Design Memory
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\upgrades\\DESIGN_INTELLIGENCE.md App Profiler, Design Router, Design Tournament, and Design Memory sections, and the existing src\\design-pipeline\\ module (index.ts, penpot-integration.ts, review-gate.ts, screenshotter.ts, storage-config.ts) as the integration point - these new pieces sit in front of the existing pipeline, they do not replace it. Create src\\design-pipeline\\app-profiler.ts exporting classifyInterface(projectPath): AppDesignProfile inferring application_type and interface_types from route structure and package.json rather than requiring manual input. Create src\\design-pipeline\\design-router.ts exporting selectDesignTools(profile): RoutingDecision using a capability-score table for whichever design tools/skills are actually installed in this environment - if no external design skill packages are installed, output routing decisions referencing only src\\design-pipeline's own screenshotter/review-gate capabilities and clearly mark external-tool slots as NOT_INSTALLED rather than fabricating tool availability. Create src\\design-pipeline\\tournament.ts exporting generateVariants(brief, count): DesignVariant[] wired to call the existing screenshotter.ts once per variant for comparison. Create src\\design-pipeline\\design-memory.ts as a CRUD module (new design_preferences table in database.ts) recording approve/reject decisions from review-gate.ts's existing approval flow. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(design): App Profiler, Design Router, Design Tournament, Design Memory layered onto existing pipeline'."
    gates:
    - type: compile

  - id: stage9-qa-tooling-gap
    name: Add Semgrep SAST, OWASP ZAP DAST, Schemathesis API contract testing
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\upgrades\\QA_TESTING_FRAMEWORK.md Semgrep, ZAP, and Schemathesis sections, and src\\testing\\runners\\security-runner.ts as the pattern reference (it currently wraps hand-rolled regex scanning via security-scanner.ts, not Semgrep). Create src\\testing\\runners\\semgrep-runner.ts running npx semgrep --config=auto --json against projectPath, mapping findings to TestRunResult using an existing or closest-match TestSuiteDb enum value - list the exact enum values from src\\memory\\test-results.ts first, do not invent one. Create src\\testing\\runners\\zap-runner.ts running OWASP ZAP baseline scan against a running preview URL if one is available in build context, otherwise skipping with a clear SKIPPED status rather than failing. Create src\\testing\\runners\\schemathesis-runner.ts running schemathesis against an OpenAPI spec if one exists in the project, otherwise SKIPPED. Register all three in orchestrator.ts runner list, gated to run only at MILESTONE and PRE-DEPLOYMENT trigger tiers per the progressive-gate concept in QA_TESTING_FRAMEWORK.md, not on every single prompt. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(testing): Semgrep SAST, OWASP ZAP DAST, Schemathesis API contract runners'."
    gates:
    - type: compile
