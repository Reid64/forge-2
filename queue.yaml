project: forge-2
governance:
- BLUEPRINT.md
- STATE_OF_THE_BUILD.md
- SESSION_STATE.md
- SCHEMA_REGISTRY.md
- BEHAVIORAL_CONTRACTS.md
prompts:
  - id: wire-real-design-tool-detection
    name: Detect installed img2threejs skill and impeccable marketplace, remove stale NOT_INSTALLED comment
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\src\\design-pipeline\\design-router.ts in full, especially the doc comment at lines 38-45 claiming none of taste_skill/impeccable/awesome_design/img2threejs are installed, and the DESIGN_TOOLS const array and capability-scoring object (impeccable at line 135, img2threejs at line 169). This comment is now stale: img2threejs was cloned to the real path join(os.homedir(),'.claude','skills','img2threejs'), and impeccable was added via claude plugin marketplace add pbakaus/impeccable. Find the real on-disk marketplace cache location Claude Code CLI uses to persist marketplace additions (search under os.homedir()\\.claude\\ - check for a marketplaces.json, config.json, or similar - do not guess the exact filename, actually look) rather than assuming a path. Add a function detectInstalledDesignTools(): Record<DesignTool, boolean> that checks: for img2threejs, whether join(os.homedir(),'.claude','skills','img2threejs') exists and contains SKILL.md; for impeccable, whether the real persisted marketplace registration you found confirms pbakaus/impeccable is present; for taste_skill and awesome_design, return false since neither has been installed in this environment - do not fabricate detection for tools that were never actually installed. Update selectDesignTools (or wherever DesignRoutingDecision is built) to call detectInstalledDesignTools() and only route to a tool if actually detected present. Remove or correct the stale doc comment to reflect current reality. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'fix(design-router): real on-disk detection for img2threejs and impeccable, remove stale not-installed comment'."
    gates:
    - type: compile

  - id: fix-contract10-branch-isolation
    name: Investigate and fix root cause of Contract 10 Branch Isolation violations
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\src\\phases\\phase3-executor.ts git merge/checkout/commit sequence in full, and src\\governance\\invariants.ts's no-direct-commits-to-main-during-build check. Two builds today (41fbf52a and 5082a219) each triggered this invariant: a non-merge commit landed directly on main mid-build. Determine the actual mechanism - check whether the orchestrator's chore commit ('orchestrator -- X marked complete') is being misclassified as a violation by the invariant checker (a false positive in the detector), or whether Phase 3's own merge flow is genuinely committing to main outside the expected forge/<id>/prompt-N branch pattern (a real process bug). Read git log --all --oneline for both build IDs to see actual commit ancestry before concluding. Fix whichever is true: either correct the invariant checker to exclude legitimate orchestrator housekeeping commits, or fix the merge flow so it never commits directly to main. Do not guess - the evidence must support the fix. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'fix(invariants): resolve Contract 10 Branch Isolation false-positive-or-real-bug based on evidence'."
    gates:
    - type: compile

  - id: fix-queue-prompt-type-metadata
    name: Fix unknown prompt_type warning appearing on every queue entry
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\src\\phases\\phase3-executor.ts where it logs queue.yaml entry has unknown prompt_type '' defaulted to 'feature'. Read the queue YAML type/schema definition to find the prompt_type field. Every queue written by hand recently omitted this field. Add a required prompt_type field to the queue schema with a documented enum of valid values (feature, fix, schema, auth, api, ui, test, docs, refactor - derived from what self-created-agents already key on in src/learning/build-brain-evolver.ts and self_created_agents trigger_conditions). Update the schema validator to fail queue loading with a clear error if prompt_type is missing, rather than silently defaulting. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'fix(queue-schema): require explicit prompt_type, stop silent feature default'."
    gates:
    - type: compile

  - id: wire-agent-approval-cli
    name: Build forge agent approve/reject/list CLI command
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\src\\cli\\index.ts printAgentLine function and the self_created_agents table schema. There is currently no command that changes an agent's status from proposed to active or rejected. Add forge agent list (shows all self_created_agents with status, using existing printAgentLine coloring), forge agent approve <id> (sets status=active, approved_at=now), forge agent reject <id> (sets status=rejected). Wire approved agents into the Phase 3 executor's prompt-sequence matching so an active agent's trigger_conditions.sequence actually gets checked against upcoming queue entries before dispatching them individually - this is the missing activation path. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(cli): forge agent approve/reject/list, wire active agents into Phase 3 dispatch'."
    gates:
    - type: compile

  - id: verify-fix-ansi-colors
    name: Verify and fix ANSI color codes in FORGE 1.0 output parity
    prompt: "Read C:\\Users\\manag\\Documents\\FORGE\\forge.ps1 Log function's exact color assignments. Read C:\\Users\\manag\\Documents\\forge-2\\src\\phases\\phase3-executor.ts's renderProgress function added in commit 6733e2a. Confirm whether actual color codes (not just field structure) were wired in - check for chalk usage or ANSI escape sequences matching FORGE 1.0's exact colors. If colors are missing or use different values than FORGE 1.0, fix renderProgress to use the identical color scheme via the chalk library already used elsewhere in this codebase (confirmed present - src/cli/index.ts uses chalk.green/yellow/dim). Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'fix(executor): wire actual ANSI colors matching FORGE 1.0 exactly, not just structure'."
    gates:
    - type: compile

  - id: validate-scratch-promote-incident
    name: Reproduce the real Tarritrix collision incident against forge-2 and prove zero conflicts
    prompt: "Read the scratch-promote implementation added in commits 993aac5 and fadc2c2 in full. Write a test file tests/integration/scratch-promote-collision.test.ts that reproduces the exact real incident: simulate two concurrent Build Agent invocations both instructed to write the same shared_canonical path (use a test path like docs/decisions/test-collision-scenario.md). Assert: both invocations respect the lock mechanism (one waits/retries rather than both writing simultaneously), both scratch-write to distinct timestamped paths, promote_scratch correctly escalates to _pending-review without auto-merging if content differs, and promotes cleanly if identical. Use real file system operations in a temp git repo fixture, not mocks. Run the new test and confirm it passes. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'test(scratch-promote): reproduce real Tarritrix collision incident, prove zero-conflict resolution'."
    gates:
    - type: compile

  - id: retrofit-tarritrix-scratch-promote
    name: Apply shared_canonical classification to Tarritrix's real manifest
    prompt: "Read C:\\Users\\manag\\Documents\\FORGE\\library\\Tarritrix\\library-manifest.yaml in full. For every queue file referenced, find any prompt whose instructions tell Claude to update STATE_OF_THE_BUILD.md, SCHEMA_REGISTRY.md, or write to docs/decisions/**. Add path_class: shared_canonical to that prompt's file_exists gate declaration, following the schema built in commit 993aac5. Do not change any other prompt content. Be conservative - only touch entries genuinely targeting the four seed patterns. List every file modified in the commit message. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'fix(tarritrix): retrofit shared_canonical classification onto STATE_OF_THE_BUILD/SCHEMA_REGISTRY/decisions prompts'."
    gates:
    - type: compile

  - id: wire-budget-circuit-breaker
    name: Build run-level budget cap enforcement
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\src\\phases\\phase3-executor.ts cost estimation logging. Add a maxBudgetUsd field to manifest.yaml schema. Before dispatching each prompt, sum actual model costs already spent this build and compare against maxBudgetUsd. If the next prompt's estimated cost would exceed the cap, halt the queue cleanly between prompts, write reason to STATE_OF_THE_BUILD.md, print BUDGET CAP REACHED in FORGE 1.0-style log format. Default maxBudgetUsd to null for backward compatibility. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(engine): run-level budget cap enforcement, halts cleanly between prompts'."
    gates:
    - type: compile

  - id: build-live-command-center
    name: Build forge dashboard live-reading command
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\src\\telemetry\\run-recorder.ts and the .forge/runs/<run-id>/*.jsonl format it writes. Add forge dashboard command to src\\cli\\index.ts that tails the most recent run's events.jsonl, prompts.jsonl, and tests.jsonl in real time and renders a live-updating terminal view: current prompt name/number, elapsed time, gate status, running cost total, recent test results. Read-only viewer, do not duplicate what run-recorder.ts already records. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(cli): forge dashboard live terminal command center reading .forge/runs JSONL'."
    gates:
    - type: compile

  - id: add-iac-sbom-license-runners
    name: Add Checkov/Terrascan IaC scanning, SBOM generation, license audit runners
    prompt: "Read src\\testing\\runners\\dependency-runner.ts as pattern reference. List exact TestSuiteDb enum values from src\\memory\\test-results.ts first. Create src\\testing\\runners\\iac-runner.ts running npx checkov -d . --output json, SKIPPED if no IaC files present. Create src\\testing\\runners\\sbom-runner.ts running trivy fs --format cyclonedx . writing SBOM artifact path into TestRunResult. Create src\\testing\\runners\\license-runner.ts running trivy fs --scanners license . flagging GPL/AGPL/SSPL as WARN, unlicensed as FAIL. Register all three, gated to MILESTONE and PRE-DEPLOYMENT tiers only. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(testing): Checkov IaC scanning, Trivy SBOM generation, license audit runners'."
    gates:
    - type: compile

  - id: add-property-based-testing-runners
    name: Add pytest/Hypothesis and fast-check property-based testing runners
    prompt: "Read src\\testing\\runners\\unit.ts as pattern reference. List exact TestSuiteDb enum values first. Create src\\testing\\runners\\python-property-runner.ts running pytest with Hypothesis for Python files, SKIPPED if none exist. Create src\\testing\\runners\\fastcheck-runner.ts that runs any existing fast-check test files found, SKIPPED with clear message if none exist - do not auto-generate property tests. Register both in orchestrator.ts. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(testing): pytest/Hypothesis and fast-check property-based testing runners'."
    gates:
    - type: compile

  - id: add-mutation-testing-runner
    name: Add Stryker mutation testing runner
    prompt: "Read src\\testing\\runners\\unit.ts as pattern reference. List exact TestSuiteDb enum values first. Create src\\testing\\runners\\mutation-runner.ts running npx stryker run --reporters json, parsing mutation score into TestRunResult. Gate to MILESTONE and ENTERPRISE_RELEASE tiers only. Generate minimal default stryker.conf scoped to src/**/*.ts if none exists rather than failing. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(testing): Stryker mutation testing runner, gated to milestone/release tiers'."
    gates:
    - type: compile

  - id: add-resilience-testing-part1
    name: Add chaos, recovery, and backup-restoration testing runners
    prompt: "Read src\\testing\\runners\\api.ts as pattern reference. List exact TestSuiteDb enum values first. Create src\\testing\\runners\\chaos-runner.ts that, only with a preview URL available (reuse zap-runner.ts's detection logic), injects latency and malformed requests, reports graceful-degradation vs crash. Create src\\testing\\runners\\recovery-runner.ts checking for a health-check endpoint, PASSES with a note if found - lightweight presence check, not crash simulation. Create src\\testing\\runners\\backup-restore-runner.ts checking governance docs for backup-strategy content, PASS/FAIL/SKIPPED - policy-presence check, not destructive drill. Register all three, gated to ENTERPRISE_RELEASE only. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(testing): chaos, recovery, backup-restore presence/behavior testing runners'."
    gates:
    - type: compile

  - id: add-resilience-testing-part2
    name: Add idempotency, concurrency, flaky-test, and test-order-dependency detection
    prompt: "Read the previous commit's chaos/recovery/backup-restore runners in full first. List exact TestSuiteDb enum values first. Create src\\testing\\runners\\idempotency-runner.ts that, with a preview URL, fires the same POST/PUT twice checking for identical response/no duplicate side effect, SKIPPED otherwise. Create src\\testing\\runners\\concurrency-runner.ts firing N simultaneous requests (default 5) checking for race-condition symptoms, SKIPPED otherwise. Create src\\testing\\flaky-detector.ts exporting detectFlakyTests(buildRunId, db) querying test_run_results for tests showing both PASS and FAIL across recent runs, flagging FLAKY; wire into TestOrchestrator result-aggregation. Create src\\testing\\test-order-detector.ts with an opt-in mode running the suite twice with randomized order, diffing pass/fail sets, default off. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(testing): idempotency/concurrency runners, flaky-test detection, opt-in test-order-dependency detection'."
    gates:
    - type: compile

  - id: design-intelligence-remainder-part1
    name: Build Brand Intelligence Engine, Persona Profiler, Aesthetic Reference Engine, Design Variance Controller
    prompt: "Read src\\design-pipeline\\app-profiler.ts and design-router.ts in full (including the detection fix from this session's first prompt). Create src\\design-pipeline\\brand-intelligence.ts exporting inferBrandProfile(projectPath): BrandIdentity reading existing content in governance docs and design_artifacts/brand_identities table rows, inferring tone/avoid-list from PRD.md/BLUEPRINT.md if no explicit brand doc exists. Create src\\design-pipeline\\persona-profiler.ts exporting inferTargetPersonas(projectPath): Persona[] from PRD.md's target-users section. Create src\\design-pipeline\\aesthetic-reference.ts exporting a self-contained AESTHETIC_FAMILIES catalog (5-8 named style directions with descriptive tokens - not tied to any external tool) and matchAestheticFamily(profile). Create src\\design-pipeline\\variance-controller.ts exporting MINIMUM_VARIANCE thresholds and validateVariance(variantA, variantB) flagging color-only/font-only/spacing-only variants as PROHIBITED. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(design): Brand Intelligence, Persona Profiler, Aesthetic Reference catalog, Design Variance Controller'."
    gates:
    - type: compile

  - id: design-intelligence-remainder-part2
    name: Build Composite Design Builder, Design-System Extractor, Component/Token Consolidator, Deployment Design Gate
    prompt: "Read the previous commit's brand-intelligence/persona-profiler/aesthetic-reference/variance-controller code in full first. Create src\\design-pipeline\\composite-builder.ts exporting buildComposite(selections, variants) allowing human-selected mix-and-match, re-rendering via existing screenshotter.ts. Create src\\design-pipeline\\design-system-extractor.ts exporting extractDesignSystem(projectPath) scanning existing component files for repeated color/spacing/typography values - real static-analysis extractor, not a stub. Create src\\design-pipeline\\token-consolidator.ts exporting consolidateTokens deduplicating near-identical values within tolerance. Create src\\design-pipeline\\deployment-gate.ts exporting checkDesignApproval(projectPath) blocking deployment unless an approved design_reviews record exists with approved_by/approved_at populated; wire as new precondition in the existing pre-deploy gate. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(design): Composite Design Builder, Design-System Extractor, Token Consolidator, Deployment Design Gate wired to pre-deploy'."
    gates:
    - type: compile

  - id: build-project-ontology-traceability
    name: Extend requirements traceability into full bidirectional ontology
    prompt: "Read src\\governance\\traceability.ts in full - extend it, do not replace it. Create src\\governance\\ontology.ts exporting typed entities (Requirement, Feature, Capability, UserStory, AcceptanceCriterion, ArchitectureComponent, Service, ApiEndpoint, DatabaseEntity, Workflow, Agent, Dependency, Test, Risk, Decision, Defect, Deployment, Environment, Release) as a lightweight typed graph, cross-referencing existing tables (risks, adr_records, test_run_results, prompt_executions) rather than new storage. Extend traceRequirement to walk the full chain bidirectionally. Add forge trace --reverse <file-path> and forge trace --untested. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(governance): full bidirectional project ontology and traceability graph'."
    gates:
    - type: compile

  - id: build-agent-permissions-sandboxing
    name: Build least-privilege agent identity and sandboxed execution
    prompt: "Read src\\integration\\bus.ts and src\\resurrection\\human-gate.ts as reference. Create src\\governance\\agent-contracts.ts exporting an AgentContract type and a registry of contracts for existing named subsystems (design-pipeline agent: can modify src/design-pipeline, cannot modify src/learning or governance docs; migration-related code: can generate migrations, cannot execute against production without approval; security runners: read-only source access). Create src\\governance\\permission-enforcer.ts exporting checkPermission(agentContext, targetPath, operation): boolean called before any Phase 3 file write, denying/logging violations that fall outside the contract. Wire as a new pre-write check alongside existing invariant checks. Static contract-boundary check against the existing single-process model, not process/container sandboxing. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(governance): least-privilege agent contracts and permission enforcement pre-write check'."
    gates:
    - type: compile

  - id: build-ephemeral-preview-environments
    name: Build ephemeral preview environment support for substantial features
    prompt: "Read src\\phases\\phase3-executor.ts's existing branch-per-prompt git flow in full - build on this, do not replace it. Create src\\deployment\\ephemeral-preview.ts exporting createPreview(branchName, projectPath) that, when VERCEL_TOKEN is present (SKIPPED cleanly if absent), triggers a Vercel preview deployment for the current branch before merge, runs TestOrchestrator's POST_PROMPT suite against the live preview URL, captures the preview URL into prompt_executions. Export teardownPreview(previewId). Wire as optional step gated by previewEnvironments flag in manifest.yaml, default false. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(deployment): ephemeral Vercel preview environments per feature branch, opt-in via manifest flag'."
    gates:
    - type: compile

  - id: build-forge-benchmark-suite
    name: Build fixed benchmark suite for measuring FORGE version-over-version improvement
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\upgrades\\ENGINEERING_COMPLETENESS.md benchmark-suite section. Create benchmarks\\ directory with benchmarks\\manifest.json listing 5 fixed lightweight benchmark scenarios: simple-crud (2-prompt queue building one CRUD route against a throwaway temp SQLite file), multi-tenant-check (1-prompt queue scored on tenant_id filtering presence), legacy-resurrection (1-prompt queue against a small pre-seeded fixture), broken-migration-fix (1-prompt queue against a fixture with malformed migration), security-remediation (1-prompt queue against a fixture with one deliberately hardcoded secret). Create src\\governance\\benchmark-runner.ts exporting runBenchmarkSuite() executing all 5 against a disposable temp directory (never real forge-2 or any real project), scoring completion/defect-rate/cost/latency, writing to benchmarks\\results\\<timestamp>.json. Register forge benchmark command. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(benchmark): fixed 5-scenario benchmark suite for measuring FORGE version-over-version improvement'."
    gates:
    - type: compile

  - id: build-shadow-mode-evaluation
    name: Build shadow-mode evaluation harness for self-improvement promotion
    prompt: "Read src\\learning\\evolution-promoter.ts in full - extend its promotion gate, do not replace it. Create src\\learning\\shadow-mode.ts exporting runShadowComparison(candidateStrategy, existingStrategy, benchmarkSuite) that, before EvolutionPromoter auto-promotes anything with confidence >=0.90, runs both strategies against the benchmark suite (import runBenchmarkSuite, do not duplicate) without either controlling real production work, comparing completion rate/defect rate/cost/latency. Only proceed to promotion if candidate strictly outperforms baseline on majority of metrics with no completion-rate regression. Wire as new required precondition inside promoteEligible, before the existing INSERT into evolution_promotions step. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(learning): shadow-mode evaluation gate before EvolutionPromoter auto-promotion'."
    gates:
    - type: compile

  - id: final-verify-and-governance-sync
    name: Full verification pass and governance doc sync for entire session
    prompt: "In C:\\Users\\manag\\Documents\\forge-2 run the full test suite (pnpm test if it exists, otherwise pnpm run build as minimum bar) and confirm 0 failures. Run the new forge benchmark command and confirm it completes without error against disposable fixtures. Update STATE_OF_THE_BUILD.md, SESSION_STATE.md reflecting every commit landed this session by id and one-line description. Update AGENTS.md with any new subsystems added as first-class agents (permission-enforcer, shadow-mode evaluator, design-tool detector). Update BEHAVIORAL_CONTRACTS.md if new invariants were added. List every TestSuiteDb enum value with a real wired runner versus still schema-only, record that count in STATE_OF_THE_BUILD.md as a measurable completion metric. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'chore: full verification pass, governance sync for entire high-priority completion session'."
    gates:
    - type: compile
