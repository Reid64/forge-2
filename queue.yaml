project: forge-2
governance:
- BLUEPRINT.md
- STATE_OF_THE_BUILD.md
- SESSION_STATE.md
- SCHEMA_REGISTRY.md
- BEHAVIORAL_CONTRACTS.md
prompts:
  - id: fix-boundary-check-false-positive
    name: Allowlist known external read-only paths in project-boundary heuristic
    prompt_type: feature
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\src\\phases\\phase3-executor.ts lines 851-861 (OUT_OF_BOUNDS_PATH_PATTERN and its doc comment) and lines 2928-2939 (where findOutOfBoundsPaths is called and used to force a prompt to fail) in full. This is a best-effort regex heuristic over Claude's raw stdout prose, explicitly documented as not a guarantee - it currently has zero allowlist and flags any absolute-looking path outside projectPath, including paths Claude only READ from and merely mentioned in explanation text, not paths it actually wrote to. Tonight this produced a false positive: detection logic legitimately needed to reference ~/.claude/skills/img2threejs/SKILL.md and ~/.claude/plugins/known_marketplaces.json as read targets, got flagged as a boundary violation anyway, triggering 10 pointless retries and dead-loop detection despite the actual code change being correct and already merged to main. Add an ALLOWLISTED_EXTERNAL_PATTERNS array/regex covering known-legitimate external read-only locations: the user home .claude/skills/** directory, .claude/plugins/** directory, and the FORGE library/projects folders under Documents/FORGE/** (locations forge-2 legitimately reads from or reports on, never writes to as build output). Update findOutOfBoundsPaths to exclude any match against this allowlist before returning violations. Do not weaken the check for genuinely out-of-bounds paths outside this allowlist. Update the doc comment to reflect the allowlist's existence and rationale. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'fix(executor): allowlist known external read-only paths in project-boundary heuristic, eliminates false-positive dead-loop trigger'."
    gates:
    - type: compile

  - id: fix-contract10-branch-isolation
    name: Investigate and fix root cause of Contract 10 Branch Isolation violations
    prompt_type: feature
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\src\\phases\\phase3-executor.ts git merge/checkout/commit sequence in full, and src\\governance\\invariants.ts's no-direct-commits-to-main-during-build check. Two builds today (41fbf52a and 5082a219) each triggered this invariant: a non-merge commit landed directly on main mid-build. Determine the actual mechanism - check whether the orchestrator's chore commit ('orchestrator -- X marked complete') is being misclassified as a violation by the invariant checker (a false positive in the detector), or whether Phase 3's own merge flow is genuinely committing to main outside the expected forge/<id>/prompt-N branch pattern (a real process bug). Read git log --all --oneline for both build IDs to see actual commit ancestry before concluding. Fix whichever is true. Do not guess - the evidence must support the fix. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'fix(invariants): resolve Contract 10 Branch Isolation false-positive-or-real-bug based on evidence'."
    gates:
    - type: compile

  - id: fix-queue-prompt-type-metadata
    name: Fix unknown prompt_type warning appearing on every queue entry
    prompt_type: feature
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\src\\phases\\phase3-executor.ts where it logs queue.yaml entry has unknown prompt_type '' defaulted to 'feature'. Read the queue YAML type/schema definition to find the prompt_type field. Add a required prompt_type field to the queue schema with a documented enum of valid values (feature, fix, schema, auth, api, ui, test, docs, refactor - derived from what self-created-agents already key on in src/learning/build-brain-evolver.ts and self_created_agents trigger_conditions). Update the schema validator to fail queue loading with a clear error if prompt_type is missing, rather than silently defaulting. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'fix(queue-schema): require explicit prompt_type, stop silent feature default'."
    gates:
    - type: compile

  - id: wire-agent-approval-cli
    name: Build forge agent approve/reject/list CLI command
    prompt_type: agent
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\src\\cli\\index.ts printAgentLine function and the self_created_agents table schema. Add forge agent list, forge agent approve <id> (sets status=active, approved_at=now), forge agent reject <id> (sets status=rejected). Wire approved agents into Phase 3 executor's prompt-sequence matching so an active agent's trigger_conditions.sequence actually gets checked against upcoming queue entries before dispatching individually. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(cli): forge agent approve/reject/list, wire active agents into Phase 3 dispatch'."
    gates:
    - type: compile

  - id: verify-fix-ansi-colors
    name: Verify and fix ANSI color codes in FORGE 1.0 output parity
    prompt_type: feature
    prompt: "Read C:\\Users\\manag\\Documents\\FORGE\\forge.ps1 Log function's exact color assignments. Read C:\\Users\\manag\\Documents\\forge-2\\src\\phases\\phase3-executor.ts's renderProgress function added in commit 6733e2a. Confirm whether actual color codes were wired in - check for chalk usage matching FORGE 1.0's exact colors. If missing or different, fix renderProgress using chalk (already used in src/cli/index.ts). Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'fix(executor): wire actual ANSI colors matching FORGE 1.0 exactly, not just structure'."
    gates:
    - type: compile

  - id: validate-scratch-promote-incident
    name: Reproduce the real Tarritrix collision incident against forge-2 and prove zero conflicts
    prompt_type: test
    prompt: "Read the scratch-promote implementation added in commits 993aac5 and fadc2c2 in full. Write tests/integration/scratch-promote-collision.test.ts reproducing the incident: simulate two concurrent Build Agent invocations both writing the same shared_canonical path (test path like docs/decisions/test-collision-scenario.md). Assert lock respect, distinct scratch writes, correct escalation on differing content, clean promotion when identical. Use real file system operations in a temp git repo fixture, not mocks. Run the test and confirm it passes. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'test(scratch-promote): reproduce real Tarritrix collision incident, prove zero-conflict resolution'."
    gates:
    - type: compile

  - id: retrofit-tarritrix-scratch-promote
    name: Apply shared_canonical classification to Tarritrix's real manifest
    prompt_type: feature
    prompt: "Read C:\\Users\\manag\\Documents\\FORGE\\library\\Tarritrix\\library-manifest.yaml in full. For every queue file referenced, find any prompt whose instructions update STATE_OF_THE_BUILD.md, SCHEMA_REGISTRY.md, or write to docs/decisions/**. Add path_class: shared_canonical to that prompt's file_exists gate declaration. Do not change other prompt content. Be conservative - only touch entries genuinely targeting the four seed patterns. List every file modified in the commit message. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'fix(tarritrix): retrofit shared_canonical classification onto governance/decisions prompts'."
    gates:
    - type: compile

  - id: wire-budget-circuit-breaker
    name: Build run-level budget cap enforcement
    prompt_type: feature
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\src\\phases\\phase3-executor.ts cost estimation logging. Add maxBudgetUsd field to manifest.yaml schema. Before dispatching each prompt, sum actual model costs already spent and compare against maxBudgetUsd. If next prompt's estimated cost would exceed the cap, halt the queue cleanly between prompts, write reason to STATE_OF_THE_BUILD.md, print BUDGET CAP REACHED. Default maxBudgetUsd to null for backward compatibility. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(engine): run-level budget cap enforcement, halts cleanly between prompts'."
    gates:
    - type: compile

  - id: build-live-command-center
    name: Build forge dashboard live-reading command
    prompt_type: feature
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\src\\telemetry\\run-recorder.ts and .forge/runs/<run-id>/*.jsonl format. Add forge dashboard command tailing the most recent run's events.jsonl, prompts.jsonl, tests.jsonl in real time, rendering a live-updating terminal view: current prompt name/number, elapsed time, gate status, running cost total, recent test results. Read-only viewer only. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(cli): forge dashboard live terminal command center reading .forge/runs JSONL'."
    gates:
    - type: compile

  - id: add-iac-sbom-license-runners
    name: Add Checkov/Terrascan IaC scanning, SBOM generation, license audit runners
    prompt_type: test
    prompt: "Read src\\testing\\runners\\dependency-runner.ts as pattern reference. List exact TestSuiteDb enum values from src\\memory\\test-results.ts first. Create src\\testing\\runners\\iac-runner.ts running npx checkov -d . --output json, SKIPPED if no IaC files present. Create src\\testing\\runners\\sbom-runner.ts running trivy fs --format cyclonedx . Create src\\testing\\runners\\license-runner.ts running trivy fs --scanners license . flagging GPL/AGPL/SSPL as WARN, unlicensed as FAIL. Register all three, gated to MILESTONE and PRE-DEPLOYMENT tiers only. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(testing): Checkov IaC scanning, Trivy SBOM generation, license audit runners'."
    gates:
    - type: compile

  - id: add-property-based-testing-runners
    name: Add pytest/Hypothesis and fast-check property-based testing runners
    prompt_type: test
    prompt: "Read src\\testing\\runners\\unit.ts as pattern reference. List exact TestSuiteDb enum values first. Create src\\testing\\runners\\python-property-runner.ts running pytest with Hypothesis for Python files, SKIPPED if none exist. Create src\\testing\\runners\\fastcheck-runner.ts running existing fast-check test files, SKIPPED with clear message if none exist. Register both. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(testing): pytest/Hypothesis and fast-check property-based testing runners'."
    gates:
    - type: compile

  - id: add-mutation-testing-runner
    name: Add Stryker mutation testing runner
    prompt_type: test
    prompt: "Read src\\testing\\runners\\unit.ts as pattern reference. List exact TestSuiteDb enum values first. Create src\\testing\\runners\\mutation-runner.ts running npx stryker run --reporters json, parsing mutation score into TestRunResult. Gate to MILESTONE and ENTERPRISE_RELEASE tiers only. Generate minimal default stryker.conf if none exists. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(testing): Stryker mutation testing runner, gated to milestone/release tiers'."
    gates:
    - type: compile

  - id: add-resilience-testing-part1
    name: Add chaos, recovery, and backup-restoration testing runners
    prompt_type: test
    prompt: "Read src\\testing\\runners\\api.ts as pattern reference. List exact TestSuiteDb enum values first. Create src\\testing\\runners\\chaos-runner.ts that, only with a preview URL available (reuse zap-runner.ts detection logic), injects latency/malformed requests, reports graceful-degradation vs crash. Create src\\testing\\runners\\recovery-runner.ts checking for a health-check endpoint. Create src\\testing\\runners\\backup-restore-runner.ts checking governance docs for backup-strategy content. Register all three, gated to ENTERPRISE_RELEASE only. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(testing): chaos, recovery, backup-restore presence/behavior testing runners'."
    gates:
    - type: compile

  - id: add-resilience-testing-part2
    name: Add idempotency, concurrency, flaky-test, and test-order-dependency detection
    prompt_type: test
    prompt: "Read the previous commit's chaos/recovery/backup-restore runners in full first. List exact TestSuiteDb enum values first. Create src\\testing\\runners\\idempotency-runner.ts firing the same POST/PUT twice checking for identical response. Create src\\testing\\runners\\concurrency-runner.ts firing N simultaneous requests checking race-condition symptoms. Create src\\testing\\flaky-detector.ts exporting detectFlakyTests flagging tests showing both PASS and FAIL across recent runs. Create src\\testing\\test-order-detector.ts with opt-in randomized-order mode, default off. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(testing): idempotency/concurrency runners, flaky-test detection, opt-in test-order-dependency detection'."
    gates:
    - type: compile

  - id: design-intelligence-remainder-part1
    name: Build Brand Intelligence Engine, Persona Profiler, Aesthetic Reference Engine, Design Variance Controller
    prompt_type: feature
    prompt: "Read src\\design-pipeline\\app-profiler.ts and design-router.ts in full (including tonight's detection fix). Create src\\design-pipeline\\brand-intelligence.ts exporting inferBrandProfile reading governance docs and brand_identities table rows. Create src\\design-pipeline\\persona-profiler.ts exporting inferTargetPersonas from PRD.md. Create src\\design-pipeline\\aesthetic-reference.ts exporting a self-contained AESTHETIC_FAMILIES catalog and matchAestheticFamily. Create src\\design-pipeline\\variance-controller.ts exporting MINIMUM_VARIANCE thresholds and validateVariance flagging color-only/font-only/spacing-only variants as PROHIBITED. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(design): Brand Intelligence, Persona Profiler, Aesthetic Reference catalog, Design Variance Controller'."
    gates:
    - type: compile

  - id: design-intelligence-remainder-part2
    name: Build Composite Design Builder, Design-System Extractor, Component/Token Consolidator, Deployment Design Gate
    prompt_type: feature
    prompt: "Read the previous commit's brand-intelligence/persona-profiler/aesthetic-reference/variance-controller code in full first. Create src\\design-pipeline\\composite-builder.ts exporting buildComposite for human-selected mix-and-match, re-rendering via existing screenshotter.ts. Create src\\design-pipeline\\design-system-extractor.ts exporting extractDesignSystem scanning existing components for repeated color/spacing/typography values. Create src\\design-pipeline\\token-consolidator.ts exporting consolidateTokens. Create src\\design-pipeline\\deployment-gate.ts exporting checkDesignApproval blocking deployment unless an approved design_reviews record exists; wire as new precondition in the existing pre-deploy gate. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(design): Composite Design Builder, Design-System Extractor, Token Consolidator, Deployment Design Gate wired to pre-deploy'."
    gates:
    - type: compile

  - id: build-project-ontology-traceability
    name: Extend requirements traceability into full bidirectional ontology
    prompt_type: feature
    prompt: "Read src\\governance\\traceability.ts in full - extend it, do not replace it. Create src\\governance\\ontology.ts exporting typed entities (Requirement, Feature, Capability, UserStory, AcceptanceCriterion, ArchitectureComponent, Service, ApiEndpoint, DatabaseEntity, Workflow, Agent, Dependency, Test, Risk, Decision, Defect, Deployment, Environment, Release) as a lightweight typed graph cross-referencing existing tables (risks, adr_records, test_run_results, prompt_executions). Extend traceRequirement to walk the full chain bidirectionally. Add forge trace --reverse <file-path> and forge trace --untested. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(governance): full bidirectional project ontology and traceability graph'."
    gates:
    - type: compile

  - id: build-agent-permissions-sandboxing
    name: Build least-privilege agent identity and sandboxed execution
    prompt_type: agent
    prompt: "Read src\\integration\\bus.ts and src\\resurrection\\human-gate.ts as reference. Create src\\governance\\agent-contracts.ts exporting an AgentContract type and a registry of contracts for existing named subsystems. Create src\\governance\\permission-enforcer.ts exporting checkPermission called before any Phase 3 file write, denying/logging violations that fall outside contract. Wire as new pre-write check alongside existing invariant checks. Static contract-boundary check, not process/container sandboxing. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(governance): least-privilege agent contracts and permission enforcement pre-write check'."
    gates:
    - type: compile

  - id: build-ephemeral-preview-environments
    name: Build ephemeral preview environment support for substantial features
    prompt_type: deploy
    prompt: "Read src\\phases\\phase3-executor.ts's existing branch-per-prompt git flow in full. Create src\\deployment\\ephemeral-preview.ts exporting createPreview that, when VERCEL_TOKEN is present (SKIPPED cleanly if absent), triggers a Vercel preview deployment before merge, runs TestOrchestrator's POST_PROMPT suite against the live preview URL, captures the preview URL into prompt_executions. Export teardownPreview. Wire as optional step gated by previewEnvironments flag in manifest.yaml, default false. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(deployment): ephemeral Vercel preview environments per feature branch, opt-in via manifest flag'."
    gates:
    - type: compile

  - id: build-forge-benchmark-suite
    name: Build fixed benchmark suite for measuring FORGE version-over-version improvement
    prompt_type: test
    prompt: "Read C:\\Users\\manag\\Documents\\forge-2\\upgrades\\ENGINEERING_COMPLETENESS.md benchmark-suite section. Create benchmarks\\manifest.json listing 5 fixed lightweight benchmark scenarios: simple-crud, multi-tenant-check, legacy-resurrection, broken-migration-fix, security-remediation - each a 1-2 prompt queue against disposable fixtures, never real forge-2 or any real project. Create src\\governance\\benchmark-runner.ts exporting runBenchmarkSuite scoring completion/defect-rate/cost/latency, writing to benchmarks\\results\\<timestamp>.json. Register forge benchmark command. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(benchmark): fixed 5-scenario benchmark suite for measuring FORGE version-over-version improvement'."
    gates:
    - type: compile

  - id: build-shadow-mode-evaluation
    name: Build shadow-mode evaluation harness for self-improvement promotion
    prompt_type: test
    prompt: "Read src\\learning\\evolution-promoter.ts in full - extend its promotion gate, do not replace it. Create src\\learning\\shadow-mode.ts exporting runShadowComparison that, before EvolutionPromoter auto-promotes anything with confidence >=0.90, runs both existing and candidate strategy against the benchmark suite (import runBenchmarkSuite, do not duplicate) without either controlling real production work. Only proceed to promotion if candidate strictly outperforms baseline with no completion-rate regression. Wire as new required precondition inside promoteEligible. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'feat(learning): shadow-mode evaluation gate before EvolutionPromoter auto-promotion'."
    gates:
    - type: compile

  - id: final-verify-and-governance-sync
    name: Full verification pass and governance doc sync for entire session
    prompt_type: test
    prompt: "In C:\\Users\\manag\\Documents\\forge-2 run the full test suite (pnpm test if it exists, otherwise pnpm run build as minimum bar) and confirm 0 failures. Run the new forge benchmark command and confirm it completes without error against disposable fixtures. Update STATE_OF_THE_BUILD.md, SESSION_STATE.md reflecting every commit landed this session by id and one-line description. Update AGENTS.md with any new subsystems added as first-class agents. Update BEHAVIORAL_CONTRACTS.md if new invariants were added. List every TestSuiteDb enum value with a real wired runner versus still schema-only, record that count in STATE_OF_THE_BUILD.md as a measurable completion metric. Run pnpm run build in C:\\Users\\manag\\Documents\\forge-2 confirm 0 errors. Commit: git add -A; git commit -m 'chore: full verification pass, governance sync for entire high-priority completion session'."
    gates:
    - type: compile
