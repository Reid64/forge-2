# forge-2 -- SESSION STATE

> Generated by FORGE 2.0 Phase 2 (Governance Generator) on 2026-07-17T00:23:28.919Z.
> Tracks the live execution session. Initialized empty at Phase 2 -- Phase 3 updates it
> after every prompt (BLUEPRINT Canonical Rule 9).

- **Current phase:** Phase 3 (Build Executor) — in progress
- **Current prompt:** 2/20 auth-setup (auth) — complete
- **Completed prompts:** 2
- **Failed prompts:** 0
- **Last updated:** 2026-07-17T00:40:20.000Z

## Active Build
Build run `324652b0-b93f-4090-b72c-05ef1eebbba6` — prompt 2/20 (auth-setup) executed:
- `middleware.ts` rewritten as a full file. Uses `@supabase/ssr` `createServerClient` with request/response cookie adapters; branches on `/api/*` vs page routes at every failure point (page → redirect `/login?next=...`, API → JSON 401/403, never a redirect — Iron Law 4).
- `supabase/migrations/004_create_profiles.sql` added: `profiles(id uuid PK → auth.users.id, role text default 'operator', created_at)`, RLS (`auth.uid() = id`), and an `on_auth_user_created` trigger that auto-provisions the row. Not yet applied against a live Supabase instance in this environment (Docker/self-hosted Supabase unreachable per TOOLCHAIN.md) — apply via `supabase db push` or Supabase MCP `apply_migration` before first login.
- `lib/supabase/server.ts`, `lib/supabase/client.ts` rewritten on `@supabase/ssr` (`createServerClient` / `createBrowserClient`), replacing the deprecated `@supabase/auth-helpers-nextjs`.
- `lib/supabase/route-auth.ts` rewritten: `requireOperatorSession()` (401 `UnauthorizedError` / 403 `ForbiddenError`, `unauthorizedResponse()` / `forbiddenResponse()`) — the double-verification every future API route handler (api-audit, api-resurrect, api-health) must call.
- `lib/auth/constants.ts` added (`OPERATOR_ROLE`, `ROLE_HEADER = 'x-user-role'`, `LOGIN_PATH`).
- `app/login/page.tsx`, `app/login/LoginForm.tsx`, `app/page.tsx`, `app/api/auth/signout/route.ts` migrated from `session.user.app_metadata.role` to a `profiles.role` query.
- `scripts/create-operator-user.mjs` and `.env.example` updated to match (role now comes from the trigger-provisioned `profiles` row, not `app_metadata`).
- `package.json`: `@supabase/auth-helpers-nextjs` removed, `@supabase/ssr` added, `@supabase/supabase-js` bumped to `^2.110.7` (peer requirement).
- Verified: `pnpm run web:typecheck` and `pnpm run web:build` both pass.
- See STATE_OF_THE_BUILD.md "Known governance-doc inconsistency" for why `gap_audit_runs`/`artifact_health_scores` did NOT get a `user_id` column or Postgres RLS despite BEHAVIORAL_CONTRACTS.md's Permissions model paragraph describing that — those two tables are SQLite-only (Build Memory), confirmed by SCHEMA_REGISTRY.md and TESTING.md; only `profiles` legitimately carries Postgres RLS.

Next: prompt 3/20 group — api-audit, api-resurrect, api-health (parallel_group `api-routes`, both depend on auth-setup).

## IDE STATUS

- **VS Code path:** not detected
- **CHANGESET.md reviewed:** NO
- **Last changeset date:** 2026-07-21T23:27:41.973Z

## Notes
Initialized empty by Phase 2 (Governance Generator). Phase 3 updates this after every prompt.
