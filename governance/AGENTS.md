# forge-2 -- AGENTS

> Generated by FORGE 2.0 Phase 2 (Governance Generator) on 2026-07-17T00:23:28.919Z.
> GOVERNANCE DOCUMENT -- read-only during Phase 3 execution (BEHAVIORAL_CONTRACTS Contract 3).

## Orchestration
No autonomous background agents are required. All audit, scoring, regeneration, and gate evaluation operations are user-initiated (via `forge audit <path>` CLI command or POST /api/audit/run API route) and complete within a single synchronous or short-lived async request/response cycle that the operator waits on. The PRD describes no recurring scheduled jobs, no webhook consumers, no queue workers, and no unattended background processes. The `GapAuditor`, `ArtifactHealthScorer`, `RegenerationEngine`, and `HumanGateEvaluator` are invoked as in-process orchestration steps within the audit pipeline, not as independent agents. The Phase 3 executor consumes the continuation plan on demand when `forge resurrect --resume` is called. All work is operator-triggered and operator-awaited.

## Agent Definitions
This project defines no autonomous agents.
