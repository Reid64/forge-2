# multi-tenant-check — FORGE 2.0 Build Queue
# Generated by the FORGE Queue Generator (s4-p02) on 2026-08-22T00:00:00.000Z.
# Working directory: C:/Users/manag/Documents/forge-2/benchmarks/fixtures/multi-tenant-check
# Each prompt is executed via: claude -p --dangerously-skip-permissions
#
# 3 prompt(s), 0 parallel group(s), longest dependency chain 3, ~7000 estimated tokens.
# Standard build order: schema → auth → api → ui → features → agents → dashboards → settings → tests → deploy → verify.
# Build HALTS after governance + queue for Gate 3 (human approval, Contract 2).

- id: schema-tenants
  name: "Schema: tenant isolation"
  prompt_type: schema
  dependencies: []
  governance_refs: [SCHEMA_REGISTRY.md]
  estimated_tokens: 2000
  context_injection:
    schema_sections: [documents, tenants]
    behavioral_sections: []
    interaction_maps: []
  description: |
    Confirm the `documents` table has a `tenant_id` column with a NOT NULL constraint and an index.

- id: api-tenant-scope
  name: "Fix tenant-scoped query"
  prompt_type: api
  dependencies: [schema-tenants]
  governance_refs: [BEHAVIORAL_CONTRACTS.md]
  estimated_tokens: 3000
  context_injection:
    schema_sections: [documents]
    behavioral_sections: [Multi-Tenancy]
    interaction_maps: []
  description: |
    Audit src/db.js's getDocuments() query and fix the missing tenant_id filter that currently leaks every tenant's rows to every other tenant.

- id: test-tenant-isolation
  name: "Test: tenant isolation"
  prompt_type: test
  dependencies: [api-tenant-scope]
  governance_refs: [TESTING.md]
  estimated_tokens: 2000
  context_injection:
    schema_sections: []
    behavioral_sections: []
    interaction_maps: ["tenants: isolation"]
  description: |
    Add a regression test proving tenant A's session cannot read tenant B's documents via getDocuments().
