# simple-crud — FORGE 2.0 Build Queue
# Generated by the FORGE Queue Generator (s4-p02) on 2026-08-22T00:00:00.000Z.
# Working directory: C:/Users/manag/Documents/forge-2/benchmarks/fixtures/simple-crud
# Each prompt is executed via: claude -p --dangerously-skip-permissions
#
# 3 prompt(s), 0 parallel group(s), longest dependency chain 3, ~7000 estimated tokens.
# Standard build order: schema → auth → api → ui → features → agents → dashboards → settings → tests → deploy → verify.
# Build HALTS after governance + queue for Gate 3 (human approval, Contract 2).

- id: schema-tasks
  name: "Schema: tasks table"
  prompt_type: schema
  dependencies: []
  governance_refs: [SCHEMA_REGISTRY.md]
  estimated_tokens: 2000
  context_injection:
    schema_sections: [tasks]
    behavioral_sections: []
    interaction_maps: []
  description: |
    Create a `tasks` table (id, title, done, created_at) via a Supabase migration under supabase/migrations/.

- id: api-tasks
  name: "API: /tasks CRUD"
  prompt_type: api
  dependencies: [schema-tasks]
  governance_refs: [BEHAVIORAL_CONTRACTS.md]
  estimated_tokens: 3000
  context_injection:
    schema_sections: [tasks]
    behavioral_sections: ["API Contracts"]
    interaction_maps: []
  description: |
    Implement REST CRUD endpoints for /tasks (GET, POST, PUT, DELETE) in src/index.js.

- id: test-tasks
  name: "Test: /tasks CRUD"
  prompt_type: test
  dependencies: [api-tasks]
  governance_refs: [TESTING.md]
  estimated_tokens: 2000
  context_injection:
    schema_sections: []
    behavioral_sections: []
    interaction_maps: ["tasks: crud"]
  description: |
    Add integration tests covering the /tasks CRUD endpoints (create, list, update, delete).
