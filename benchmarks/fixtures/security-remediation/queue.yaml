# security-remediation — FORGE 2.0 Build Queue
# Generated by the FORGE Queue Generator (s4-p02) on 2026-08-22T00:00:00.000Z.
# Working directory: C:/Users/manag/Documents/forge-2/benchmarks/fixtures/security-remediation
# Each prompt is executed via: claude -p --dangerously-skip-permissions
#
# 3 prompt(s), 0 parallel group(s), longest dependency chain 3, ~6000 estimated tokens.
# Standard build order: schema → auth → api → ui → features → agents → dashboards → settings → tests → deploy → verify.
# Build HALTS after governance + queue for Gate 3 (human approval, Contract 2).

- id: scan-secrets
  name: "Scan for secrets + injection"
  prompt_type: feature
  dependencies: []
  governance_refs: []
  estimated_tokens: 1500
  context_injection:
    schema_sections: []
    behavioral_sections: []
    interaction_maps: []
  description: |
    Scan src/api.js for hardcoded secrets and SQL injection patterns; list every finding with file/line.

- id: remediate-secrets
  name: "Remediate findings"
  prompt_type: feature
  dependencies: [scan-secrets]
  governance_refs: []
  estimated_tokens: 3000
  context_injection:
    schema_sections: []
    behavioral_sections: []
    interaction_maps: []
  description: |
    Remove the hardcoded Stripe key from src/api.js (source it from process.env.STRIPE_SECRET_KEY instead) and parameterize the SQL query in findUserByEmail() to eliminate the injection vector.

- id: test-security
  name: "Test: no secrets, no injection"
  prompt_type: test
  dependencies: [remediate-secrets]
  governance_refs: [TESTING.md]
  estimated_tokens: 1500
  context_injection:
    schema_sections: []
    behavioral_sections: []
    interaction_maps: []
  description: |
    Add a test asserting no hardcoded secret pattern remains in src/api.js and that the user-lookup query is parameterized.
