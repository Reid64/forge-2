# broken-migration-fix — FORGE 2.0 Build Queue
# Generated by the FORGE Queue Generator (s4-p02) on 2026-08-22T00:00:00.000Z.
# Working directory: C:/Users/manag/Documents/forge-2/benchmarks/fixtures/broken-migration-fix
# Each prompt is executed via: claude -p --dangerously-skip-permissions
#
# 3 prompt(s), 0 parallel group(s), longest dependency chain 3, ~5500 estimated tokens.
# Standard build order: schema → auth → api → ui → features → agents → dashboards → settings → tests → deploy → verify.
# Build HALTS after governance + queue for Gate 3 (human approval, Contract 2).

- id: diagnose-migration
  name: "Diagnose broken migration"
  prompt_type: schema
  dependencies: []
  governance_refs: [SCHEMA_REGISTRY.md]
  estimated_tokens: 1500
  context_injection:
    schema_sections: [users, orders]
    behavioral_sections: []
    interaction_maps: []
  description: |
    Diagnose why supabase/migrations/0001_init.sql fails to apply — identify every syntax and reference error.

- id: fix-migration
  name: "Fix broken migration"
  prompt_type: schema
  dependencies: [diagnose-migration]
  governance_refs: [SCHEMA_REGISTRY.md]
  estimated_tokens: 2500
  context_injection:
    schema_sections: [users, orders]
    behavioral_sections: []
    interaction_maps: []
  description: |
    Fix supabase/migrations/0001_init.sql so it applies cleanly against a fresh Postgres/Supabase database, preserving the intended users/orders schema.

- id: test-migration
  name: "Test: migration applies"
  prompt_type: test
  dependencies: [fix-migration]
  governance_refs: [TESTING.md]
  estimated_tokens: 1500
  context_injection:
    schema_sections: []
    behavioral_sections: []
    interaction_maps: []
  description: |
    Add a migration-apply smoke test that fails loudly if 0001_init.sql regresses.
