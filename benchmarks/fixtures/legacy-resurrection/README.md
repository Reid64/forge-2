# legacy-resurrection fixture

Status: abandoned since 2019. Last known good build used Node 8.

This is a FORGE benchmark fixture, not a real project. It exists to test whether FORGE
can resurrect a small, stale codebase: deprecated dependencies (`request`), a
callback-style API nobody has touched in years, and no tests.
