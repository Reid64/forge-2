# legacy-resurrection — FORGE 2.0 Build Queue
# Generated by the FORGE Queue Generator (s4-p02) on 2026-08-22T00:00:00.000Z.
# Working directory: C:/Users/manag/Documents/forge-2/benchmarks/fixtures/legacy-resurrection
# Each prompt is executed via: claude -p --dangerously-skip-permissions
#
# 3 prompt(s), 0 parallel group(s), longest dependency chain 3, ~7000 estimated tokens.
# Standard build order: schema → auth → api → ui → features → agents → dashboards → settings → tests → deploy → verify.
# Build HALTS after governance + queue for Gate 3 (human approval, Contract 2).

- id: audit-legacy
  name: "Audit abandoned module"
  prompt_type: feature
  dependencies: []
  governance_refs: []
  estimated_tokens: 2500
  context_injection:
    schema_sections: []
    behavioral_sections: []
    interaction_maps: []
  description: |
    Audit the abandoned src/legacy.js module (untouched since 2019, depends on the deprecated/archived "request" package) and produce a short resurrection plan.

- id: modernize-legacy
  name: "Modernize legacy.js"
  prompt_type: feature
  dependencies: [audit-legacy]
  governance_refs: []
  estimated_tokens: 3000
  context_injection:
    schema_sections: []
    behavioral_sections: []
    interaction_maps: []
  description: |
    Modernize src/legacy.js: replace the deprecated "request" callback API with a modern fetch()-based implementation and restore a working, dependency-free entry point.

- id: test-legacy
  name: "Test: resurrected module"
  prompt_type: test
  dependencies: [modernize-legacy]
  governance_refs: [TESTING.md]
  estimated_tokens: 1500
  context_injection:
    schema_sections: []
    behavioral_sections: []
    interaction_maps: []
  description: |
    Add a smoke test proving the resurrected fetchStatus() runs without throwing on a modern Node install.
