{
  "playwrightSpecs": [
    {
      "name": "Grant Source Type Badge and Filter Flow",
      "file": "e2e/grant-source-type.spec.ts",
      "scenario": "1. Log in as admin user. 2. Navigate to /grants. 3. Assert at least one grant row is visible. 4. Assert SourceTypeBadge component renders with correct color class for 'federal_government' (bg-blue-*). 5. Click source type filter panel, select 'private_foundation'. 6. Assert URL contains 'source_type=private_foundation'. 7. Assert all visible badge chips show 'Private Foundation'. 8. Navigate to /grants/[id] for a grant. 9. Click 'Source Type' dropdown. 10. Select 'corporate_foundation'. 11. Assert optimistic badge update to 'Corporate Foundation' with teal color class. 12. Assert toast 'Source type updated.' appears. 13. Reload page and assert badge persists as 'Corporate Foundation'."
    },
    {
      "name": "Research Run Parallel Execution and Deduplication",
      "file": "e2e/research-run.spec.ts",
      "scenario": "1. Log in as admin user. 2. Navigate to /research. 3. Assert 'Run Research' button is enabled. 4. Click 'Run Research'. 5. Assert button becomes disabled with tooltip 'A research run is already in progress.' 6. Assert progress panel renders agent cards each showing 'Queued' then transitioning to 'Running' state via Supabase Realtime. 7. Wait for run completion (timeout 120s). 8. Assert completion toast: 'Research run complete. [K] new grants discovered.' OR warning toast if partial failure. 9. Assert summary banner shows deduplication stats (total_found, duplicates_removed, unique_saved). 10. Navigate to /grants and assert new grants appear in list. 11. Attempt POST /api/research/run again while run is in-progress (intercept and assert 409 response in prior step)."
    },
    {
      "name": "Search Profile Configuration Full Flow",
      "file": "e2e/search-profile.spec.ts",
      "scenario": "1. Log in as admin user. 2. Navigate to /settings/search-profile. 3. Assert form renders with default values (loading skeleton then content). 4. Toggle off 'Grants' funding type, assert only 'Grants' toggle is off. 5. Toggle all funding types off, attempt save, assert inline error 'At least one funding type must be selected.'. 6. Toggle 'Grants' back on. 7. Set amount_min to 50000 and amount_max to 10000, assert inline error 'Minimum amount cannot exceed maximum amount.'. 8. Set amount_max to 500000, assert error clears and display shows '$50,000 – $500,000'. 9. Select geographic scope 'State-specific', assert state picker appears. 10. Select 'CA' and 'NY', assert chips appear. 11. Add negative filter tag 'religious', press Enter, assert chip appears. 12. Add duplicate 'religious', assert it is highlighted but not duplicated. 13. Click 'Save Search Profile', assert toast 'Search profile saved. Your next research run will use these settings.' 14. Reload page, assert all saved values persist. 15. Click 'Reset to Defaults', confirm modal, assert form resets to defaults without saving. 16. Click save, assert defaults are persisted."
    },
    {
      "name": "Analytics Dashboard Charts Render and Filter",
      "file": "e2e/analytics-dashboard.spec.ts",
      "scenario": "1. Log in as admin user. 2. Navigate to /analytics. 3. Assert page title 'Analytics' is visible. 4. Assert all chart panels render: Pipeline Funnel, Success Rate card, Dollars Requested vs Awarded, Source Category Pie, Deadline Heatmap, Agent Activity. 5. Assert loading skeletons appear then resolve. 6. Select 'Last 30 days' preset, assert URL updates with from/to params. 7. Assert FunnelChart renders stage labels (Discovered, Applied, Awarded). 8. Assert Success Rate card shows percentage or '—' with explanatory text. 9. Click a pie chart slice (e.g., 'Private Foundation'), assert grant table below filters and active filter chip appears. 10. Click a deadline heatmap day cell with grants, assert grant table filters to grants due that day. 11. Navigate to /outcomes, assert 301 redirect to /analytics."
    },
    {
      "name": "Eligibility Scoring and High Match Badge",
      "file": "e2e/eligibility-scoring.spec.ts",
      "scenario": "1. Log in as admin user. 2. Ensure search profile is configured (navigate to /settings/search-profile, save). 3. Navigate to /grants. 4. Assert grants with eligibility_flag='high_match' show green '⭐ High Match' badge. 5. Assert grants with eligibility_flag='unscored' show 'Unscored' chip. 6. Filter by 'High Match' eligibility filter, assert URL contains 'eligibility_flag=high_match', assert only high_match grants visible. 7. Navigate to /grants/[id] for a high_match grant. 8. Click 'View Eligibility Details', assert panel expands showing score circular indicator, rationale text, scoring timestamp, and 'Re-score' button. 9. Click 'Re-score', assert button disabled with spinner. 10. Assert success toast 'Eligibility re-scored.' and score/rationale update."
    },
    {
      "name": "Alerts Center Read and Mark All Read",
      "file": "e2e/alerts.spec.ts",
      "scenario": "1. Log in as admin user. 2. Assert sidebar 'Alerts' icon shows red badge with unread count. 3. Navigate to /alerts. 4. Assert alert list renders with unread alerts highlighted. 5. Click a single alert, assert POST /api/alerts/[id]/read is called and alert loses unread styling. 6. Assert sidebar badge count decrements. 7. Click 'Mark All Read', assert POST /api/alerts/read-all is called. 8. Assert all alerts show as read and sidebar badge disappears. 9. Assert empty state if no unread alerts: 'You're all caught up.'"
    },
    {
      "name": "Consensus Validation Request and Result Display",
      "file": "e2e/consensus-validation.spec.ts",
      "scenario": "1. Log in as admin user. 2. Navigate to /grants/[id] for a discovered grant. 3. Assert 'Validate with AI Consensus' button is visible. 4. Click button, assert POST /api/consensus/validate is called. 5. Assert loading state while validation runs. 6. Assert consensus result panel renders showing: primary model score (Claude), secondary model score (GPT-4o), consensus score, agreement indicator (agree/disagree), and combined rationale. 7. Assert if models agree (within 10 points) a green 'Consensus Reached' badge shows. 8. Assert if models disagree a yellow 'Models Disagree' badge shows with individual scores. 9. Navigate away and back, assert GET /api/consensus/[grant_id] loads persisted result."
    },
    {
      "name": "Role-Based Access Control Enforcement",
      "file": "e2e/rbac.spec.ts",
      "scenario": "1. Log in as viewer role user. 2. Navigate to /grants/[id]. 3. Assert 'Source Type' dropdown is read-only/disabled. 4. Assert 'Re-score' button is not visible. 5. Navigate to /settings/search-profile, assert form fields are disabled and save button is not rendered. 6. Navigate to /settings/team, assert invite controls are not visible. 7. Log out. 8. Log in as member role user. 9. Navigate to /settings/team, assert member cannot access team management. 10. Assert member CAN trigger research run. 11. Assert member CANNOT access /settings/organization admin controls."
    },
    {
      "name": "Multi-Tenant Data Isolation",
      "file": "e2e/tenant-isolation.spec.ts",
      "scenario": "1. Log in as admin of Organization A. 2. Create a grant for Organization A via UI. 3. Note grant ID. 4. Log out. 5. Log in as admin of Organization B. 6. Attempt to navigate to /grants/[grant_id_from_org_a]. 7. Assert 404 or redirect to /grants (grant not found). 8. Call GET /api/grants directly, assert Organization A's grant is not in response. 9. Attempt PATCH /api/grants/[org_a_grant_id] via fetch, assert 403 or 404 response. 10. Assert Organization B's grant list contains only Organization B's grants."
    },
    {
      "name": "Concurrent Research Run Prevention",
      "file": "e2e/concurrent-research.spec.ts",
      "scenario": "1. Log in as admin user. 2. Directly INSERT a research_runs row with status='running' for this organization via test setup. 3. Navigate to /research. 4. Assert 'Run Research' button is disabled. 5. Hover button, assert tooltip text 'A research run is already in progress.' 6. Intercept POST /api/research/run, assert it returns 409. 7. Assert toast 'A research run is already in progress. Please wait for it to complete.' 8. Clean up: update research_runs row to status='completed'. 9. Assert 'Run Research' button becomes enabled."
    }
  ],
  "apiTests": [
    {
      "route": "GET /api/grants",
      "cases": [
        "Returns 200 with grants array scoped to authenticated user's organization_id only",
        "Returns 401 when no auth token provided",
        "Returns empty array (not 404) when organization has no grants",
        "Filters by source_type=federal_government query param, returns only matching grants",
        "Filters by eligibility_flag=high_match query param, returns only matching grants",
        "Filters by multiple source_types (comma-separated), returns union of matching grants",
        "Does NOT return grants from other organizations even if grant IDs are guessed",
        "Returns 400 with validation error when source_type param is not a valid enum value",
        "Respects pagination via page and limit query params, returns correct slice",
        "Returns grants with source_type, match_percentage, eligibility_flag, eligibility_notes fields populated"
      ]
    },
    {
      "route": "GET /api/grants/[id]",
      "cases": [
        "Returns 200 with full grant object including source_type, match_percentage, eligibility_flag, eligibility_notes for own organization grant",
        "Returns 404 when grant belongs to different organization (tenant isolation via RLS)",
        "Returns 401 when no auth token provided",
        "Returns 404 when grant ID does not exist"
      ]
    },
    {
      "route": "PATCH /api/grants/[id]",
      "cases": [
        "Returns 200 and updates source_type when valid enum value provided and user owns grant",
        "Returns 400 when source_type value is not a valid enum member",
        "Returns 403 or 404 when patching grant belonging to different organization",
        "Returns 401 when no auth token provided",
        "Returns 403 when authenticated user has viewer role",
        "Returns 404 when grant ID does not exist",
        "Ignores unknown fields in request body (does not throw)",
        "Returns updated grant object in response body"
      ]
    },
    {
      "route": "POST /api/grants/[id]/rescore",
      "cases": [
        "Returns 200 and triggers eligibility re-scoring for own organization grant",
        "Returns updated match_percentage, eligibility_flag, eligibility_notes in response",
        "Returns 404 when grant belongs to different organization",
        "Returns 401 when no auth token provided",
        "Returns 403 when user has viewer role",
        "Returns 400 when no search profile is configured for organization (includes descriptive error message)",
        "Returns 404 when grant ID does not exist",
        "Sets eligibility_scored_at timestamp on successful rescore"
      ]
    },
    {
      "route": "POST /api/research/run",
      "cases": [
        "Returns 201 and creates research_runs row with status=running when no run is in progress",
        "Returns 409 when a research_runs row with status=running already exists for organization",
        "Returns 401 when no auth token provided",
        "Returns 403 when user has viewer role",
        "Accepts optional search_profile_id and associates it with the research_runs row",
        "Creates research_run_agents rows for each active agent in the organization",
        "Returns 400 when organization has no active agents configured"
      ]
    },
    {
      "route": "GET /api/research/runs",
      "cases": [
        "Returns 200 with array of research_runs scoped to authenticated organization",
        "Returns 401 when no auth token provided",
        "Returns runs with deduplication_stats JSONB populated after completion",
        "Returns runs in descending started_at order by default",
        "Does not return runs from other organizations"
      ]
    },
    {
      "route": "GET /api/research/runs/[id]",
      "cases": [
        "Returns 200 with full run object including research_run_agents array",
        "Returns 404 when run belongs to different organization",
        "Returns 401 when no auth token provided",
        "Returns 404 when run ID does not exist"
      ]
    },
    {
      "route": "GET /api/search-profiles",
      "cases": [
        "Returns 200 with search profile for authenticated organization",
        "Returns 404 when no profile exists for organization",
        "Returns 401 when no auth token provided",
        "Returns profile with all fields: funding_types, source_type_filters, focus_area_weights, amount_min, amount_max, geographic_scope, eligibility_filters, negative_filters"
      ]
    },
    {
      "route": "POST /api/search-profiles",
      "cases": [
        "Returns 200 and creates new search_profiles row when none exists for organization",
        "Returns 200 and updates existing search_profiles row (upsert by organization_id)",
        "Returns 401 when no auth token provided",
        "Returns 403 when user has viewer role",
        "Returns 400 when funding_types array is empty",
        "Returns 400 when amount_min exceeds amount_max",
        "Returns 400 when geographic_scope type is state-specific but states array is empty",
        "Treats amount_max of 0 as NULL (no upper limit) and stores NULL in database",
        "Sets updated_at timestamp on every upsert",
        "Strips unknown fields from request body before saving"
      ]
    },
    {
      "route": "GET /api/analytics/summary",
      "cases": [
        "Returns 200 with summary metrics for authenticated organization",
        "Returns 401 when no auth token provided",
        "Accepts from and to query params (ISO date strings) and filters data to date range",
        "Returns zero-value metrics (not errors) when organization has no grants",
        "Does not include data from other organizations"
      ]
    },
    {
      "route": "GET /api/analytics/funnel",
      "cases": [
        "Returns 200 with counts per grant status stage scoped to organization",
        "Returns 401 when no auth token provided",
        "Returns all funnel stages with count 0 when no grants exist (not missing stages)",
        "Accepts date range params and filters accordingly"
      ]
    },
    {
      "route": "GET /api/analytics/financials",
      "cases": [
        "Returns 200 with monthly aggregated amount_requested and amount_awarded",
        "Returns 401 when no auth token provided",
        "Skips NULL amount rows without throwing errors",
        "Returns empty array (not error) when no financial data exists",
        "Accepts from and to query params and returns correct monthly buckets"
      ]
    },
    {
      "route": "GET /api/analytics/by-source",
      "cases": [
        "Returns 200 with source_type breakdown including count and total_amount per type",
        "Returns 401 when no auth token provided",
        "Returns only source types present in organization's grants",
        "Correctly aggregates grants with NULL source_type under 'other' category"
      ]
    },
    {
      "route": "GET /api/analytics/deadlines",
      "cases": [
        "Returns 200 with date-count pairs for grants with deadlines in next 90 days",
        "Returns 401 when no auth token provided",
        "Returns empty array (not error) when no upcoming deadlines exist",
        "Does not include deadlines from other organizations",
        "Only includes dates in the future (not past deadlines)"
      ]
    },
    {
      "route": "GET /api/analytics/agent-activity",
      "cases": [
        "Returns 200 with daily research run and grant discovery stats",
        "Returns 401 when no auth token provided",
        "Returns empty array (not error) when no research runs exist",
        "Includes duplicates_removed count per day from deduplication_stats"
      ]
    },
    {
      "route": "GET /api/alerts",
      "cases": [
        "Returns 200 with alerts array scoped to authenticated organization",
        "Returns 401 when no auth token provided",
        "Returns unread alerts before read alerts (sorted by read status then created_at desc)",
        "Does not return alerts from other organizations",
        "Returns correct unread_count in response metadata"
      ]
    },
    {
      "route": "POST /api/alerts/[id]/read",
      "cases": [
        "Returns 200 and marks alert as read for own organization alert",
        "Returns 404 when alert belongs to different organization",
        "Returns 401 when no auth token provided",
        "Returns 404 when alert ID does not exist",
        "Is idempotent: marking already-read alert returns 200 without error"
      ]
    },
    {
      "route": "POST /api/alerts/read-all",
      "cases": [
        "Returns 200 and marks all unread alerts as read for authenticated organization",
        "Returns 401 when no auth token provided",
        "Returns 200 with affected_count=0 when all alerts already read",
        "Only marks alerts read for the authenticated organization, not others"
      ]
    },
    {
      "route": "POST /api/consensus/validate",
      "cases": [
        "Returns 201 and triggers consensus validation for own organization grant",
        "Returns 401 when no auth token provided",
        "Returns 403 when user has viewer role",
        "Returns 404 when grant_id belongs to different organization",
        "Returns response with claude_score, gpt4o_score, consensus_score, agreement_status, combined_rationale fields",
        "Returns 400 when grant_id is missing from request body",
        "Stores validation result in consensus_validations table with correct organization_id"
      ]
    },
    {
      "route": "GET /api/consensus/[grant_id]",
      "cases": [
        "Returns 200 with most recent consensus validation result for own organization grant",
        "Returns 404 when no validation exists for the grant",
        "Returns 404 when grant belongs to different organization",
        "Returns 401 when no auth token provided",
        "Returns validation with claude_score, gpt4o_score, consensus_score, agreement_status, combined_rationale, validated_at"
      ]
    }
  ],
  "sixLawsPlan": [
    {
      "law": "Law 1 – Schema: Every multi-tenant table has company_id (organization_id), RLS enabled, and a company-scoped policy",
      "verification": "Automated: Run a Jest/Vitest database test that connects to the Supabase test instance and executes: (1) `SELECT tablename FROM pg_tables WHERE schemaname='public'` to enumerate all tables. (2) For each table in [grants, organizations, users, research_runs, research_run_agents, search_profiles, alerts, consensus_validations, agents], assert column `organization_id` exists via `SELECT column_name FROM information_schema.columns WHERE table_name=$1 AND column_name='organization_id'`. (3) Assert RLS is enabled via `SELECT relrowsecurity FROM pg_class WHERE relname=$1` = true for each table. (4) Assert at least one RLS policy exists per table via `SELECT count(*) FROM pg_policies WHERE tablename=$1` > 0. (5) Execute a raw Supabase query as a JWT belonging to Organization B attempting to SELECT from `grants` where organization_id = Organization A's ID — assert zero rows returned. This test runs in CI on every pull request."
    },
    {
      "law": "Law 2 – API: Every route validates auth, enforces tenant scope, and returns correct HTTP status codes",
      "verification": "Automated: Vitest API integration tests using `supertest` against a locally running Next.js dev server (or Vercel preview URL in CI). Test matrix: (1) Every route in the API routes list is called without Authorization header — assert 401. (2) Every mutating route (PATCH, POST) is called with a valid JWT for Organization B targeting Organization A's resource IDs — assert 403 or 404. (3) Every route with organization-scoped filtering is called with a valid JWT and asserted to never return rows with a different organization_id. (4) Viewer role JWT is used against admin-only routes (POST /api/research/run, PATCH /api/grants/[id], POST /api/search-profiles, POST /api/consensus/validate) — assert 403. (5) All 409, 400, 404 status codes verified in the apiTests section above. These tests run in CI against every PR."
    },
    {
      "law": "Law 3 – UI: Every page renders without hydration errors, all interactive components are accessible, and loading/error/empty states are implemented",
      "verification": "Automated: (1) Playwright `page.on('console', msg => { if (msg.type()==='error') throw })` attached in every e2e spec to catch hydration errors. (2) axe-playwright (`@axe-core/playwright`) accessibility scan run on every page route: /, /dashboard, /grants, /grants/[id], /research, /analytics, /alerts, /settings/search-profile, /settings/organization, /settings/team — assert zero critical/serious violations. (3) Each page spec intercepts API routes to return empty arrays and asserts empty state components render (not blank/broken). (4) Each page spec intercepts API routes to return 500 and asserts error toast or error boundary renders. (5) `next build` must complete with zero TypeScript errors (strict mode) — enforced in CI. (6) All Recharts components verified wrapped in `dynamic(() => import(...), { ssr: false })` via static code analysis grep in CI pipeline."
    },
    {
      "law": "Law 4 – Data: All writes are validated before persistence, default values are applied, and no orphaned records can exist",
      "verification": "Automated: (1) Vitest unit tests on all Zod/validation schemas used in API routes — test valid payloads pass and invalid payloads return structured errors with correct field paths. (2) Database-level constraint tests: attempt INSERT into `grants` with invalid `source_type` enum value — assert Postgres constraint violation. Attempt INSERT with NULL `organization_id` — assert NOT NULL constraint violation. (3) Test that `research_runs` row with `status=running` older than 10 minutes is correctly marked `failed` by the cron cleanup job. (4) Test deduplication logic unit: provide array of grants with duplicate URLs and duplicate funder+program combos, assert output contains only unique grants with correct metadata merge (highest confidence, union tags). (5) Test backfill default: existing grants rows without `source_type` show `other`, without `eligibility_flag` show `unscored`. (6) Foreign key constraint tests: attempt INSERT into `research_run_agents` with non-existent `research_run_id` — assert FK violation."
    },
    {
      "law": "Law 5 – Wiring: Every frontend component correctly calls its backing API route, all Realtime subscriptions connect and update UI, and all agent integrations fire correctly",
      "verification": "Automated: (1) Playwright network intercept tests: for each page, intercept API calls and assert correct route is called with correct method, query params, and request body. (e.g., /settings/search-profile save button asserts POST /api/search-profiles is called with all form fields in body). (2) Supabase Realtime wiring test: in research run e2e spec, mock a research_run_agents row update via direct DB write and assert UI agent card status badge updates within 5 seconds without page refresh. (3) Agent integration tests (Jest mocked AI providers): mock Anthropic Claude and OpenAI GPT-4o SDK responses, trigger POST /api/research/run, assert ResearchAgent calls Claude with correct prompt structure containing search profile parameters. Assert EligibilityScoringAgent is called after grant INSERT with organization eligibility_filters. Assert ConsensusValidationAgent calls both Claude and GPT-4o and stores both scores. Assert AlertGenerationAgent INSERTs alert row when eligibility_flag=high_match. (4) Optimistic update test: PATCH /api/grants/[id] is intercepted and delayed 2s — assert badge updates immediately, then reverts if 500 is returned."
    },
    {
      "law": "Law 6 – Verification: A human reviewer confirms the complete feature set matches the PRD, all acceptance criteria are met, and no regression in existing functionality",
      "verification": "Human (QA Engineer + Product Owner sign-off required before production deploy): (1) QA Engineer executes the full Playwright e2e suite on the Vercel preview deployment and records pass/fail. (2) Product Owner performs manual walkthrough of all 7 features against the PRD interaction maps: Grant Source Categorization (1.1–1.5), Research Parallel Execution (2.1–2.5), Search Profile Config (3.1–3.10), Analytics Dashboard (4.1–4.8), Eligibility Enhancement (5.1–5.7), Alerts Center, Consensus Validation. (3) QA Engineer verifies color-coded badges match exact Tailwind classes specified (federal=blue, state=indigo, local=violet, foundation=emerald, etc.). (4) QA Engineer verifies Recharts charts render correctly on Chrome, Firefox, and Safari (no SSR hydration artifacts). (5) Product Owner confirms /outcomes redirects to /analytics with 301. (6) QA Engineer verifies tenant isolation manually by creating two test organizations and confirming zero data leakage. (7) Product Owner confirms empty states, error states, and loading skeletons are present on all dashboard panels. (8) Sign-off recorded in deployment checklist in project Notion. Deploy blocked until both sign-offs are recorded."
    }
  ],
  "markdown": "# Testing Strategy\n\n## Overview\n\nThis testing strategy covers the full Benavora Tier 2 feature set across Playwright end-to-end specs, API integration tests, and a Six Laws verification plan. All tests target the FORGE default stack: Next.js 14 App Router, Supabase (Postgres + Auth + RLS), Vercel, pnpm, TypeScript strict.\n\n---\n\n## Playwright End-to-End Specifications\n\nAll specs live in `e2e/` and run via `pnpm exec playwright test` against the Vercel preview deployment on every pull request.\n\n### Spec 1 – Grant Source Type Badge and Filter Flow\n**File:** `e2e/grant-source-type.spec.ts`\n\nCovers Feature 1 (Grant Source Categorization) end-to-end:\n- Badge color class rendering per source type\n- Filter-by-source-type URL param behavior\n- Manual source type update via PATCH with optimistic UI and toast feedback\n- Persistence after page reload\n\n### Spec 2 – Research Run Parallel Execution and Deduplication\n**File:** `e2e/research-run.spec.ts`\n\nCovers Feature 2 (Parallel Research Execution):\n- Agent progress panel real-time state transitions via Supabase Realtime\n- Completion toast variants (success / partial / all-failed)\n- Deduplication stats summary banner\n- Concurrent run prevention (409 enforcement + disabled button)\n\n### Spec 3 – Search Profile Configuration Full Flow\n**File:** `e2e/search-profile.spec.ts`\n\nCovers Feature 3 (Search Profile Configuration):\n- All form sections: funding types, source filters, focus areas, dollar range, geographic scope, eligibility filters, negative filters\n- Validation errors (empty funding types, min > max, state-specific with no states)\n- Duplicate tag prevention\n- Save, persist, and reset-to-defaults flows\n\n### Spec 4 – Analytics Dashboard Charts Render and Filter\n**File:** `e2e/analytics-dashboard.spec.ts`\n\nCovers Feature 4 (Analytics Dashboard):\n- All six chart panels render without error\n- Date range selection updates URL params\n- Pie chart slice click filters grant table below\n- Deadline heatmap cell click filters grant table\n- /outcomes → /analytics redirect (HTTP 301)\n\n### Spec 5 – Eligibility Scoring and High Match Badge\n**File:** `e2e/eligibility-scoring.spec.ts`\n\nCovers Feature 5 (Automated Eligibility Enhancement):\n- Badge variants per eligibility_flag value\n- Filter-by-eligibility-flag flow\n- Eligibility detail panel expand/collapse\n- Manual re-score button with spinner and toast feedback\n\n### Spec 6 – Alerts Center Read and Mark All Read\n**File:** `e2e/alerts.spec.ts`\n\nCovers the Alerts feature:\n- Sidebar badge unread count\n- Individual alert mark-read\n- Bulk mark-all-read\n- Empty state rendering\n\n### Spec 7 – Consensus Validation Request and Result Display\n**File:** `e2e/consensus-validation.spec.ts`\n\nCovers the Consensus Validation feature:\n- Validate button triggers POST /api/consensus/validate\n- Result panel shows Claude score, GPT-4o score, consensus score, agreement badge\n- Result persists on page reload (GET /api/consensus/[grant_id])\n\n### Spec 8 – Role-Based Access Control Enforcement\n**File:** `e2e/rbac.spec.ts`\n\nCovers auth roles [admin, member, viewer]:\n- Viewer cannot edit source type, rescore, or configure search profile\n- Member cannot manage team settings but can run research\n- UI controls hidden/disabled per role\n\n### Spec 9 – Multi-Tenant Data Isolation\n**File:** `e2e/tenant-isolation.spec.ts`\n\nCovers Six Laws Law 1:\n- Cross-organization grant access returns 404\n- Cross-organization PATCH returns 403/404\n- Grant list never contains another organization's grants\n\n### Spec 10 – Concurrent Research Run Prevention\n**File:** `e2e/concurrent-research.spec.ts`\n\nCovers Feature 2.5:\n- Button disabled state during active run\n- 409 response handling\n- Button re-enables after run completes\n\n---\n\n## API Test Specifications\n\nAll API tests use Vitest + supertest against a locally running Next.js server (or Vercel preview in CI). Each route is tested for authentication, tenant isolation, valid inputs, and error cases.\n\n| Route | Key Cases |\n|---|---|\n| `GET /api/grants` | Auth, tenant scope, source_type filter, eligibility_flag filter, pagination |\n| `GET /api/grants/[id]` | Auth, own-org 200, other-org 404 |\n| `PATCH /api/grants/[id]` | Auth, valid enum, invalid enum 400, cross-tenant 403/404, viewer 403 |\n| `POST /api/grants/[id]/rescore` | Auth, success with score fields, no profile 400, viewer 403 |\n| `POST /api/research/run` | Auth, 201 creates run, 409 on concurrent, viewer 403, no agents 400 |\n| `GET /api/research/runs` | Auth, tenant scope, deduplication_stats present |\n| `GET /api/research/runs/[id]` | Auth, includes agents array, cross-tenant 404 |\n| `GET /api/search-profiles` | Auth, 200 with all fields, 404 when none exists |\n| `POST /api/search-profiles` | Auth, upsert, empty funding_types 400, min>max 400, state scope validation |\n| `GET /api/analytics/summary` | Auth, date range params, zero-value metrics for empty org |\n| `GET /api/analytics/funnel` | Auth, all stages returned with 0 counts when empty |\n| `GET /api/analytics/financials` | Auth, NULL amount handling, monthly buckets |\n| `GET /api/analytics/by-source` | Auth, NULL source_type → 'other', tenant scope |\n| `GET /api/analytics/deadlines` | Auth, future dates only, empty array not error |\n| `GET /api/analytics/agent-activity` | Auth, duplicates_removed included, empty array |\n| `GET /api/alerts` | Auth, sorted by read/created_at, tenant scope, unread_count metadata |\n| `POST /api/alerts/[id]/read` | Auth, cross-tenant 404, idempotent |\n| `POST /api/alerts/read-all` | Auth, only own-org alerts, affected_count=0 safe |\n| `POST /api/consensus/validate` | Auth, response fields, cross-tenant 404, viewer 403 |\n| `GET /api/consensus/[grant_id]` | Auth, cross-tenant 404, 404 when none exists, all result fields |\n\nFull case details are enumerated in the `apiTests` array above.\n\n---\n\n## Six Laws Verification Plan\n\n### Law 1 – Schema Integrity (Automated)\n**Verification:** Jest/Vitest database tests connect to the Supabase test instance and assert:\n- `organization_id` column exists on every multi-tenant table: `grants`, `research_runs`, `research_run_agents`, `search_profiles`, `alerts`, `consensus_validations`, `agents`\n- RLS is enabled (`relrowsecurity=true`) on each of these tables\n- At least one RLS policy exists per table\n- A cross-tenant SELECT (JWT from Org B querying Org A data) returns zero rows\n\nRuns on every pull request in CI.\n\n### Law 2 – API Contract Integrity (Automated)\n**Verification:** Vitest + supertest integration tests cover:\n- Every route returns 401 without auth token\n- Every mutating route returns 403/404 for cross-tenant resource access\n- Viewer role JWT blocked from all admin/member-only routes\n- All HTTP status codes match specification (409 concurrent, 400 validation, 404 not-found)\n\nRuns on every pull request in CI.\n\n### Law 3 – UI Integrity (Automated)\n**Verification:**\n- `page.on('console', ...)` catches hydration errors in all Playwright specs\n- axe-playwright accessibility scan on all 13 page routes (zero critical/serious violations)\n- Empty state and error state verified by intercepting APIs to return 200/[] and 500 respectively\n- `next build` TypeScript strict mode must succeed (zero errors) in CI\n- Static analysis grep confirms all Recharts components use `dynamic(..., { ssr: false })`\n\n### Law 4 – Data Integrity (Automated)\n**Verification:**\n- Zod schema unit tests for every API route request body\n- Database constraint tests: invalid enum INSERT, NULL organization_id INSERT, orphaned FK INSERT\n- Deduplication logic unit test: URL normalization, Levenshtein fuzzy match, metadata merge\n- Cron job test: research_runs rows >10 min old with status=running are marked failed\n- Default value tests: existing grants show `source_type='other'` and `eligibility_flag='unscored'` after migration\n\n### Law 5 – Wiring Integrity (Automated)\n**Verification:**\n- Playwright network intercept assertions: every form submit calls correct API route with correct payload\n- Supabase Realtime test: direct DB write to `research_run_agents` triggers UI card update within 5 seconds\n- Agent integration tests with mocked AI providers: assert correct prompts, correct provider calls (Claude for primary, GPT-4o for consensus), correct DB writes after each agent\n- Optimistic update rollback test: delayed + failed PATCH causes badge revert\n\n### Law 6 – Human Verification (Manual, Required for Production Deploy)\n**Verification:** QA Engineer + Product Owner sign-off required:\n1. Full Playwright suite pass on Vercel preview\n2. Manual walkthrough of all 7 PRD features against interaction maps\n3. Badge color verification against exact Tailwind classes from PRD\n4. Cross-browser Recharts rendering check (Chrome, Firefox, Safari)\n5. /outcomes → /analytics redirect confirmed\n6. Manual tenant isolation check with two test organizations\n7. Empty/error/loading states confirmed on all dashboard panels\n8. Sign-offs recorded in deployment checklist before production deploy\n\n---\n\n## CI Pipeline Integration\n\n```\n# .github/workflows/ci.yml (summary)\njobs:\n  type-check:     next build (TypeScript strict)\n  unit-tests:     vitest run (schema, validation, deduplication, agents)\n  api-tests:      vitest run --config vitest.api.config.ts (supertest)\n  db-tests:       vitest run --config vitest.db.config.ts (RLS, constraints)\n  e2e-tests:      playwright test (all 10 specs against preview URL)\n  accessibility:  axe-playwright scan on all page routes\n```\n\nAll jobs must pass before merge to main. Production deploy additionally requires Law 6 human sign-off.\n\n---\n\n## Test Data Management\n\n- **Seed script:** `scripts/seed-test-data.ts` creates two isolated test organizations (Org A, Org B) with:\n  - 50 grants each with varied source_types, eligibility_flags, statuses, and amounts\n  - 3 completed research_runs with deduplication_stats\n  - 1 search_profile per org\n  - 10 alerts each (5 read, 5 unread)\n  - 5 consensus_validations\n- **Cleanup:** Each Playwright spec uses `test.beforeEach` to reset to a known seed state via `POST /api/test/reset` (only available in `NODE_ENV=test`)\n- **AI mocking:** All AI provider calls are mocked via `vi.mock('@anthropic-ai/sdk')` and `vi.mock('openai')` in unit/integration tests; real providers only called in manual QA runs"
}