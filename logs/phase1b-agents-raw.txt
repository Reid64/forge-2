{
  "agents": [
    {
      "name": "ResearchAgent",
      "purpose": "Discover grant opportunities for an organization by querying AI with organization context, search profile parameters, and source type classification instructions. Returns structured grant data including source_type classification.",
      "trigger": "POST /api/research/run — spawned as one Promise per active agent configuration via Promise.allSettled after a user initiates a research run. Each agent instance receives its own configuration slice.",
      "inputContract": "{ organization_id: string, agent_config_id: string, search_profile: { funding_types: string[], source_type_filters: string[], focus_area_weights: Record<string,number>, amount_min: number|null, amount_max: number|null, geographic_scope: { type: string, states?: string[] }, eligibility_filters: object, negative_filters: string[] }, agent_instructions: string, run_id: string }",
      "outputContract": "{ agent_config_id: string, run_id: string, status: 'completed'|'failed', error?: string, grants: Array<{ title: string, funder_name: string, program_name: string, description: string, url: string|null, amount_min: number|null, amount_max: number|null, deadline: string|null, source_type: 'federal_government'|'state_government'|'local_government'|'private_foundation'|'corporate_foundation'|'community_foundation'|'international'|'faith_based'|'individual_donor'|'other', confidence_score: number, tags: string[] }> }",
      "systemPrompt": "You are a nonprofit grant research specialist. Your task is to discover real, currently-active grant opportunities that match the organization's funding profile. For each grant you discover, you MUST classify it into exactly one source_type from this list: federal_government, state_government, local_government, private_foundation, corporate_foundation, community_foundation, international, faith_based, individual_donor, other. Base classification on the funder's name, type, and description. Apply the organization's search profile strictly: honor funding_types, geographic_scope, amount ranges, focus_area_weights (higher weight = higher priority), and negative_filters (exclude any grant matching these keywords, funder names, or regions). Return ONLY a valid JSON array of grant objects. Each object must include: title (string), funder_name (string), program_name (string), description (string, 2-4 sentences), url (string or null), amount_min (integer cents or null), amount_max (integer cents or null), deadline (ISO 8601 date string or null), source_type (enum value from list above), confidence_score (float 0.0-1.0 representing your certainty this grant is real and currently active), tags (array of strings). If you cannot find real grants matching the profile, return an empty array. Never fabricate grant details — only include opportunities you have strong knowledge about.",
      "model": "claude-opus-4-5",
      "tokenBudget": 4096
    },
    {
      "name": "EligibilityScoringAgent",
      "purpose": "Score a single grant's eligibility match for a specific organization using the organization's search profile and the grant's full details. Returns a 0-100 match score, a flag category, and a brief rationale.",
      "trigger": "Called asynchronously after each grant INSERT following deduplication in a research run, and on-demand via POST /api/grants/[id]/rescore. Invoked as a Supabase Edge Function or Next.js API background task.",
      "inputContract": "{ grant_id: string, organization_id: string, grant: { title: string, funder_name: string, program_name: string, description: string, source_type: string, amount_min: number|null, amount_max: number|null, deadline: string|null, tags: string[] }, eligibility_filters: { is_501c3: boolean, years_in_operation_min: number|null, annual_budget_min: number|null, annual_budget_max: number|null, staff_size_min: number|null, staff_size_max: number|null, served_population_tags: string[] }, focus_area_weights: Record<string,number>, amount_min: number|null, amount_max: number|null, geographic_scope: object }",
      "outputContract": "{ grant_id: string, score: number, eligibility_flag: 'high_match'|'moderate_match'|'low_match'|'unscored', rationale: string, scored_at: string }",
      "systemPrompt": "You are a nonprofit eligibility assessment specialist. Your task is to score how well a specific grant opportunity matches an organization's profile and needs. Analyze the grant details against the organization's eligibility filters, focus area weights, budget requirements, geographic scope, and funding preferences. Return a JSON object with exactly these fields: score (integer 0-100, where 100 is a perfect match), rationale (string of 2-3 sentences explaining the score, citing specific matching or mismatching criteria). Scoring guidelines: 80-100 indicates strong alignment across eligibility requirements, focus areas, geography, and funding amounts; 50-79 indicates partial alignment with some gaps; 0-49 indicates significant misalignment or likely ineligibility. Be specific in your rationale — mention the grant name, key matching factors, and any disqualifying criteria. If eligibility_filters indicate the organization lacks a required characteristic (e.g., grant requires 501c3 and org may not qualify), score accordingly. Return ONLY valid JSON with no additional text.",
      "model": "claude-opus-4-5",
      "tokenBudget": 1024
    },
    {
      "name": "ConsensusValidationAgent",
      "purpose": "Provide a second-opinion validation of a grant opportunity using OpenAI GPT-4o as a secondary AI model. Compares against the primary Claude assessment to produce a consensus confidence score and flag discrepancies.",
      "trigger": "Called via POST /api/consensus/validate — triggered manually by user on Grant Detail page or automatically for grants with confidence_score < 0.7 from ResearchAgent. Runs after initial grant save.",
      "inputContract": "{ grant_id: string, organization_id: string, grant: { title: string, funder_name: string, program_name: string, description: string, url: string|null, source_type: string, amount_min: number|null, amount_max: number|null, deadline: string|null, tags: string[] }, primary_assessment: { source_type: string, confidence_score: number, eligibility_flag: string, eligibility_notes: string } }",
      "outputContract": "{ grant_id: string, secondary_model: string, secondary_source_type: 'federal_government'|'state_government'|'local_government'|'private_foundation'|'corporate_foundation'|'community_foundation'|'international'|'faith_based'|'individual_donor'|'other', secondary_confidence_score: number, secondary_eligibility_score: number, secondary_rationale: string, consensus_score: number, source_type_agreement: boolean, eligibility_agreement: boolean, discrepancy_flag: boolean, discrepancy_notes: string|null, validated_at: string }",
      "systemPrompt": "You are a grant opportunity validator performing an independent second-opinion review. You will be given details about a grant opportunity and asked to independently assess it WITHOUT being influenced by any prior assessment scores — analyze the grant on its own merits. Your tasks: 1) Classify the grant source_type from this exact list: federal_government, state_government, local_government, private_foundation, corporate_foundation, community_foundation, international, faith_based, individual_donor, other. 2) Assign a confidence_score (float 0.0-1.0) for whether this grant is real and currently active based on your knowledge. 3) Assign an eligibility_score (integer 0-100) for how well the grant matches the organization profile provided. 4) Provide a rationale (2-3 sentences). Return ONLY a valid JSON object with fields: secondary_source_type (string enum value), secondary_confidence_score (float), secondary_eligibility_score (integer), secondary_rationale (string). Be objective and independent — do not anchor to any previously mentioned scores.",
      "model": "gpt-4o",
      "tokenBudget": 1024
    },
    {
      "name": "AlertGenerationAgent",
      "purpose": "Analyze completed research runs and eligibility scoring results to generate structured alerts for high-match grants, approaching deadlines, and research run summaries. Inserts alert records into the alerts table.",
      "trigger": "Triggered automatically after research_runs.status transitions to 'completed' or 'partial' via Supabase Database Webhook or Next.js API post-processing hook. Also triggered after EligibilityScoringAgent completes for grants with eligibility_flag='high_match' that are <24 hours old.",
      "inputContract": "{ organization_id: string, run_id: string, trigger_type: 'run_completed'|'high_match_found'|'deadline_approaching', run_summary?: { total_found: number, duplicates_removed: number, unique_saved: number, agents_succeeded: number, agents_failed: number }, new_high_match_grants?: Array<{ grant_id: string, title: string, funder_name: string, match_percentage: number, deadline: string|null }>, approaching_deadline_grants?: Array<{ grant_id: string, title: string, funder_name: string, deadline: string, days_until_deadline: number }> }",
      "outputContract": "{ alerts_created: Array<{ alert_type: 'run_completed'|'high_match_grant'|'deadline_approaching'|'agent_error', organization_id: string, title: string, body: string, priority: 'high'|'medium'|'low', grant_id: string|null, run_id: string|null, metadata: object }> }",
      "systemPrompt": "You are a notification content writer for a nonprofit grant management platform. Your task is to generate clear, concise, and actionable alert messages based on research run results and grant scoring events. For each alert, create a title (max 80 characters) and body (max 200 characters) that are informative and motivate action. Alert type guidelines: run_completed — summarize what was found and prompt review (e.g., 'Research Complete: 12 new grants found, 3 high matches'); high_match_grant — highlight the specific opportunity with urgency if deadline is <30 days (e.g., '⭐ High Match: [Funder] [Program] — 92% match score'); deadline_approaching — create urgency based on days remaining (≤7 days = high priority, 8-30 days = medium priority); agent_error — explain what failed without technical jargon. Assign priority: high for ≤7 days deadlines or ≥90% match scores, medium for 8-30 days or 70-89% matches, low for informational. Return ONLY a valid JSON object matching the outputContract schema. Generate one alert object per logical event — do not batch multiple high-match grants into a single alert.",
      "model": "claude-haiku-4-5",
      "tokenBudget": 512
    }
  ],
  "orchestration": "## Agent Orchestration Flow\n\n### Primary Flow: Research Run Orchestration\n\n1. **Trigger:** User clicks 'Run Research' → `POST /api/research/run`\n2. **Pre-flight check:** API checks for existing `research_runs` row with `status='running'` for the `organization_id`; returns HTTP 409 if found.\n3. **Setup:** INSERT `research_runs` row (`status='running'`, `started_at=now()`). INSERT one `research_run_agents` row per active agent config (`status='pending'`).\n4. **Parallel Execution:** All ResearchAgent instances launched simultaneously via `Promise.allSettled`. Each agent updates its `research_run_agents` row to `status='running'` on start, then `status='completed'|'failed'` on finish. Supabase Realtime pushes these status changes to the frontend in real-time.\n5. **Deduplication:** After `Promise.allSettled` resolves, the orchestrator aggregates all results and runs the deduplication pipeline (URL normalization → URL grouping → fuzzy name matching → metadata merging). Stats written to `research_runs.deduplication_stats`.\n6. **Persistence:** Unique grants INSERTed into `grants` table with `source_type` from ResearchAgent classification.\n7. **Eligibility Scoring (async, non-blocking):** For each newly inserted grant, EligibilityScoringAgent is dispatched asynchronously. If no search profile exists, grant is marked `eligibility_flag='unscored'`. Scoring does NOT block the research run completion response.\n8. **Run Completion:** `research_runs.status` updated to `completed` or `partial`. Frontend receives completion signal.\n9. **Alert Generation:** AlertGenerationAgent triggered with run summary. For each grant where EligibilityScoringAgent returns `high_match` and grant age <24h, AlertGenerationAgent also fires a `high_match_grant` alert.\n10. **Consensus Validation (on-demand or auto for low-confidence grants):** Grants with `confidence_score < 0.7` from ResearchAgent automatically enqueue ConsensusValidationAgent. Users can also trigger manually via Grant Detail page → `POST /api/consensus/validate`.\n\n### Secondary Flow: Manual Rescore\n1. User clicks 'Re-score' on Grant Detail page → `POST /api/grants/[id]/rescore`\n2. EligibilityScoringAgent runs synchronously (within API response timeout of 30s).\n3. `grants` row updated; response returned to frontend.\n\n### Secondary Flow: Consensus Validation\n1. Triggered automatically for low-confidence grants after research run, or manually via UI.\n2. ConsensusValidationAgent (GPT-4o) runs independently of primary assessment.\n3. Orchestrator computes `consensus_score = (primary_confidence + secondary_confidence) / 2` and sets `source_type_agreement = primary_source_type === secondary_source_type` and `discrepancy_flag = |primary_eligibility_score - secondary_eligibility_score| > 20`.\n4. Results written to `consensus_validations` table. `grants.consensus_validated=true` flag set.\n\n### Timeout and Error Handling\n- Each ResearchAgent promise has a 90-second hard timeout; on timeout, agent marked `failed` with `error='timeout'`.\n- AI provider 429 responses trigger one retry after 5 seconds; second 429 marks agent `failed` with `error='rate_limited'`.\n- A Vercel cron job (every 10 minutes) scans for `research_runs` with `status='running'` and `started_at < now() - interval '10 minutes'`; marks them `failed`.\n- EligibilityScoringAgent failures do not block grant persistence; grant remains `eligibility_flag='unscored'`.\n- AlertGenerationAgent failures are logged but do not affect core research flow; alerts can be regenerated on next run.",
  "markdown": "# Agent Architecture\n\n## Overview\n\nBenavora's AI capabilities are implemented as four discrete agents, each with a single responsibility, deterministic I/O contracts, and explicit failure modes. Agents are orchestrated by Next.js API routes and Supabase Edge Functions; no dedicated orchestration framework is required at Tier 2 scale.\n\n**AI Providers:**\n- **Primary:** Anthropic Claude (claude-opus-4-5) — ResearchAgent, EligibilityScoringAgent\n- **Secondary:** OpenAI GPT-4o — ConsensusValidationAgent (second-opinion validation per PRD requirement)\n- **Utility:** Anthropic Claude Haiku (claude-haiku-4-5) — AlertGenerationAgent (low-latency, low-cost text generation)\n\n---\n\n## Agent 1: ResearchAgent\n\n**Purpose:** Discover grant opportunities by querying the primary AI provider with organization context and search profile parameters. Classifies each discovered grant by `source_type`.\n\n**Model:** `claude-opus-4-5` | **Token Budget:** 4,096\n\n**Trigger:** Spawned N-times in parallel (one per active agent configuration) via `Promise.allSettled` when `POST /api/research/run` is called.\n\n**Input Contract:**\n```typescript\n{\n  organization_id: string;\n  agent_config_id: string;\n  search_profile: {\n    funding_types: string[];\n    source_type_filters: string[];\n    focus_area_weights: Record<string, number>;\n    amount_min: number | null;\n    amount_max: number | null;\n    geographic_scope: { type: string; states?: string[] };\n    eligibility_filters: object;\n    negative_filters: string[];\n  };\n  agent_instructions: string;\n  run_id: string;\n}\n```\n\n**Output Contract:**\n```typescript\n{\n  agent_config_id: string;\n  run_id: string;\n  status: 'completed' | 'failed';\n  error?: string;\n  grants: Array<{\n    title: string;\n    funder_name: string;\n    program_name: string;\n    description: string;\n    url: string | null;\n    amount_min: number | null; // integer cents\n    amount_max: number | null; // integer cents\n    deadline: string | null;   // ISO 8601\n    source_type: SourceTypeEnum;\n    confidence_score: number;  // 0.0–1.0\n    tags: string[];\n  }>;\n}\n```\n\n**Timeout:** 90 seconds. On timeout, `research_run_agents.status` set to `failed`, `error='timeout'`.\n**Retry:** One retry on HTTP 429 after 5-second delay; second failure sets `error='rate_limited'`.\n\n---\n\n## Agent 2: EligibilityScoringAgent\n\n**Purpose:** Score a single grant's eligibility match against the organization's search profile. Assigns a 0–100 score, an `eligibility_flag` enum value, and a 2–3 sentence rationale.\n\n**Model:** `claude-opus-4-5` | **Token Budget:** 1,024\n\n**Trigger:**\n1. Asynchronously after each grant INSERT following research run deduplication.\n2. Synchronously on-demand via `POST /api/grants/[id]/rescore`.\n\n**Input Contract:**\n```typescript\n{\n  grant_id: string;\n  organization_id: string;\n  grant: {\n    title: string;\n    funder_name: string;\n    program_name: string;\n    description: string;\n    source_type: string;\n    amount_min: number | null;\n    amount_max: number | null;\n    deadline: string | null;\n    tags: string[];\n  };\n  eligibility_filters: {\n    is_501c3: boolean;\n    years_in_operation_min: number | null;\n    annual_budget_min: number | null;\n    annual_budget_max: number | null;\n    staff_size_min: number | null;\n    staff_size_max: number | null;\n    served_population_tags: string[];\n  };\n  focus_area_weights: Record<string, number>;\n  amount_min: number | null;\n  amount_max: number | null;\n  geographic_scope: object;\n}\n```\n\n**Output Contract:**\n```typescript\n{\n  grant_id: string;\n  score: number;                    // integer 0–100\n  eligibility_flag: 'high_match' | 'moderate_match' | 'low_match' | 'unscored';\n  rationale: string;\n  scored_at: string;                // ISO 8601\n}\n```\n\n**Flag Thresholds:** score ≥ 80 → `high_match`; 50–79 → `moderate_match`; < 50 → `low_match`.\n**Guard:** If no search profile exists for the organization, agent is skipped; grant set to `eligibility_flag='unscored'` with note: \"Configure a Search Profile to enable eligibility scoring.\"\n**Post-action:** If result is `high_match` and grant is < 24 hours old, AlertGenerationAgent is triggered with `trigger_type='high_match_found'`.\n\n---\n\n## Agent 3: ConsensusValidationAgent\n\n**Purpose:** Provide an independent second-opinion assessment of a grant opportunity using OpenAI GPT-4o, enabling cross-model consensus scoring and discrepancy detection.\n\n**Model:** `gpt-4o` | **Token Budget:** 1,024\n\n**Trigger:**\n1. Automatically for grants where ResearchAgent returned `confidence_score < 0.7`.\n2. Manually via `POST /api/consensus/validate` from the Grant Detail page UI.\n\n**Input Contract:**\n```typescript\n{\n  grant_id: string;\n  organization_id: string;\n  grant: {\n    title: string;\n    funder_name: string;\n    program_name: string;\n    description: string;\n    url: string | null;\n    source_type: string;\n    amount_min: number | null;\n    amount_max: number | null;\n    deadline: string | null;\n    tags: string[];\n  };\n  primary_assessment: {\n    source_type: string;\n    confidence_score: number;\n    eligibility_flag: string;\n    eligibility_notes: string;\n  };\n}\n```\n\n**Output Contract:**\n```typescript\n{\n  grant_id: string;\n  secondary_model: string;               // 'gpt-4o'\n  secondary_source_type: SourceTypeEnum;\n  secondary_confidence_score: number;    // 0.0–1.0\n  secondary_eligibility_score: number;   // 0–100\n  secondary_rationale: string;\n  consensus_score: number;               // avg of primary + secondary confidence\n  source_type_agreement: boolean;\n  eligibility_agreement: boolean;\n  discrepancy_flag: boolean;             // true if |primary - secondary eligibility| > 20\n  discrepancy_notes: string | null;\n  validated_at: string;                  // ISO 8601\n}\n```\n\n**Consensus Computation (orchestrator-side):**\n- `consensus_score = (primary_confidence_score + secondary_confidence_score) / 2`\n- `source_type_agreement = primary_source_type === secondary_source_type`\n- `eligibility_agreement = Math.abs(primary_eligibility_score - secondary_eligibility_score) <= 20`\n- `discrepancy_flag = !eligibility_agreement`\n- Results written to `consensus_validations` table; `grants.consensus_validated = true`.\n\n---\n\n## Agent 4: AlertGenerationAgent\n\n**Purpose:** Generate human-readable, actionable alert titles and body text for research run completions, high-match grant discoveries, approaching deadlines, and agent errors.\n\n**Model:** `claude-haiku-4-5` | **Token Budget:** 512\n\n**Trigger:**\n1. After `research_runs.status` transitions to `completed` or `partial` (`trigger_type='run_completed'`).\n2. After EligibilityScoringAgent scores a grant as `high_match` and grant age < 24 hours (`trigger_type='high_match_found'`).\n3. Daily cron job for grants with `deadline` within 7 or 30 days (`trigger_type='deadline_approaching'`).\n\n**Input Contract:**\n```typescript\n{\n  organization_id: string;\n  run_id: string;\n  trigger_type: 'run_completed' | 'high_match_found' | 'deadline_approaching';\n  run_summary?: {\n    total_found: number;\n    duplicates_removed: number;\n    unique_saved: number;\n    agents_succeeded: number;\n    agents_failed: number;\n  };\n  new_high_match_grants?: Array<{\n    grant_id: string;\n    title: string;\n    funder_name: string;\n    match_percentage: number;\n    deadline: string | null;\n  }>;\n  approaching_deadline_grants?: Array<{\n    grant_id: string;\n    title: string;\n    funder_name: string;\n    deadline: string;\n    days_until_deadline: number;\n  }>;\n}\n```\n\n**Output Contract:**\n```typescript\n{\n  alerts_created: Array<{\n    alert_type: 'run_completed' | 'high_match_grant' | 'deadline_approaching' | 'agent_error';\n    organization_id: string;\n    title: string;       // max 80 chars\n    body: string;        // max 200 chars\n    priority: 'high' | 'medium' | 'low';\n    grant_id: string | null;\n    run_id: string | null;\n    metadata: object;\n  }>;\n}\n```\n\n**Priority Rules:**\n- `high`: deadline ≤ 7 days OR match_percentage ≥ 90\n- `medium`: deadline 8–30 days OR match_percentage 70–89\n- `low`: informational (run completions, low-urgency events)\n\n---\n\n## Orchestration Flow\n\n### Research Run Orchestration (Primary Flow)\n\n```\nUser: POST /api/research/run\n  │\n  ├─ [Guard] Check research_runs for status='running' → 409 if found\n  ├─ INSERT research_runs (status='running')\n  ├─ INSERT research_run_agents (one per active agent, status='pending')\n  │\n  ├─ Promise.allSettled([\n  │     ResearchAgent(config1),   ← updates research_run_agents row in real-time\n  │     ResearchAgent(config2),   ← Supabase Realtime pushes to frontend\n  │     ResearchAgent(config3),\n  │  ])\n  │\n  ├─ Deduplication Pipeline\n  │     1. Normalize URLs (strip UTM, trailing slash, lowercase)\n  │     2. Group by normalized URL, keep first occurrence\n  │     3. Fuzzy match by (funder_name, program_name) — Levenshtein ≤ 3\n  │     4. Merge metadata (union tags, highest confidence_score)\n  │     5. Check existing grants table for org — skip if already tracked\n  │     6. Write research_runs.deduplication_stats\n  │\n  ├─ INSERT unique grants into grants table\n  │\n  ├─ UPDATE research_runs.status → 'completed'|'partial'\n  │\n  ├─ [Async, non-blocking] EligibilityScoringAgent for each new grant\n  │     └─ On high_match + age < 24h → AlertGenerationAgent(high_match_found)\n  │\n  ├─ AlertGenerationAgent(run_completed)\n  │\n  └─ [Auto] ConsensusValidationAgent for grants with confidence_score < 0.7\n```\n\n### Manual Rescore Flow\n```\nUser: POST /api/grants/[id]/rescore\n  └─ EligibilityScoringAgent (synchronous, 30s timeout)\n        └─ UPDATE grants row\n```\n\n### Consensus Validation Flow\n```\nUser: POST /api/consensus/validate  (or auto-triggered)\n  └─ ConsensusValidationAgent (GPT-4o, independent assessment)\n        └─ Orchestrator computes consensus_score, discrepancy_flag\n              └─ INSERT consensus_validations row\n                    └─ UPDATE grants.consensus_validated = true\n```\n\n---\n\n## Error Handling Summary\n\n| Agent | Timeout | Retry | Failure Mode |\n|---|---|---|---|\n| ResearchAgent | 90s | 1x on 429 (5s delay) | Agent marked `failed`; run continues with other agents |\n| EligibilityScoringAgent | 30s | None | Grant saved with `eligibility_flag='unscored'` |\n| ConsensusValidationAgent | 30s | None | `consensus_validations` row skipped; grant remains without consensus |\n| AlertGenerationAgent | 15s | None | Alerts skipped; logged to server console |\n\n**Stale Run Recovery:** Vercel cron (every 10 minutes) marks `research_runs` with `status='running'` and `started_at < now() - interval '10 minutes'` as `failed`.\n\n---\n\n## Database Tables Supporting Agents\n\n| Table | Purpose | Tenant-Scoped |\n|---|---|---|\n| `research_runs` | One row per run; tracks status, timing, dedup stats | Yes (`organization_id`) |\n| `research_run_agents` | One row per agent per run; tracks per-agent status and raw results | Yes (via `research_runs.organization_id`) |\n| `grants` | Persisted grant opportunities with `source_type`, `match_percentage`, `eligibility_flag`, `eligibility_notes`, `eligibility_scored_at`, `consensus_validated` | Yes (`organization_id`) |\n| `consensus_validations` | Stores ConsensusValidationAgent output per grant | Yes (`organization_id`) |\n| `alerts` | Stores AlertGenerationAgent output; read by `/api/alerts` | Yes (`organization_id`) |\n| `search_profiles` | Organization search profile consumed by ResearchAgent and EligibilityScoringAgent | Yes (`organization_id`) |\n\nAll tables have RLS enabled with `organization_id`-scoped policies enforcing tenant isolation."
}